import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import CookieComplianceView from '../cookie-compliance.view';
const mockConfig = {
    className: 'ms-cookie-compliance',
    content: 'Simple text'
};
describe('CookieCompliance Unit Test', () => {
    it('View renders correctly', () => {
        const moduleProps = buildMockModuleProps({}, {}, mockConfig);
        const mockProps = {
            ...moduleProps,
            CookieComplianceBanner: {
                moduleProps: moduleProps,
                className: mockConfig.content,
            },
            AlertProps: {
                className: 'ms-cookie-compliace__container',
                color: 'white',
            },
            Content: {
                className: 'ms-cookie-compliance__content'
            },
            text: React.createElement("div", null, "TextComponent"),
            links: (React.createElement("div", null,
                React.createElement("a", null, "Link"),
                React.createElement("a", null, "Link"),
                React.createElement("a", null, "Link"))),
            acceptButton: React.createElement("div", null, "AcceptButton")
        };
        const cookieComplianceView = render(React.createElement(CookieComplianceView, Object.assign({}, mockProps)));
        expect(cookieComplianceView).toMatchSnapshot();
    });
});
//# sourceMappingURL=cookie-compliance.view.test.js.map