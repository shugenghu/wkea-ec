import { getPayloadObject, getTelemetryAttributes } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const CookieComplianceAcceptButton = ({ acceptCookiesButtonText, acceptCookiesButtonAriaLabel, telemetryContent, onClose }) => {
    const payLoad = getPayloadObject('click', telemetryContent, 'accept cookies');
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return (React.createElement("button", Object.assign({ type: 'button', className: 'ms-cookie-compliance__accept-button msc-btn', "aria-label": acceptCookiesButtonAriaLabel, onClick: onClose }, attributes), acceptCookiesButtonText));
};
//# sourceMappingURL=accept-button.js.map