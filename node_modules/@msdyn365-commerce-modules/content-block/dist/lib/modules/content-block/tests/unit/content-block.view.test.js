import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import ContentBlockView from '../../content-block.view';
describe('Content block unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const mockProps = {
            ...moduleProps,
            title: 'HeadingComponent',
            text: 'TextComponent',
            links: 'LinksComponent',
            image: 'imageComponent',
            className: 'hero',
            contentBlockContainer: {
                moduleProps,
                className: 'ms-content-block'
            },
            imageContainer: {
                className: 'ms-content-block__image'
            },
            detailsContainer: {
                className: 'ms-content-block__details'
            },
            contentBlockAnchorTag: {
                tag: 'a',
                className: 'ms-content-block__link',
                role: 'link'
            },
            imageLink: '/modern/storefaq',
            imageAriaLabel: 'image aria label'
        };
        const component = render(React.createElement(ContentBlockView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=content-block.view.test.js.map