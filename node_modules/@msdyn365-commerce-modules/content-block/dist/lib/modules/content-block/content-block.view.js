import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const ContentBlockView = (props) => {
    const { contentBlockContainer, imageContainer, detailsContainer, title, text, links, image, contentBlockAnchorTag, imageLink, imageAriaLabel } = props;
    if (imageLink) {
        return (React.createElement(Module, Object.assign({}, contentBlockContainer),
            React.createElement(Node, Object.assign({}, contentBlockAnchorTag, { href: imageLink, className: contentBlockAnchorTag ? contentBlockAnchorTag.className : '', "aria-label": imageAriaLabel }),
                React.createElement(Node, Object.assign({}, imageContainer), image)),
            React.createElement(Node, Object.assign({}, detailsContainer),
                title,
                text,
                links)));
    }
    else {
        return (React.createElement(Module, Object.assign({}, contentBlockContainer),
            React.createElement(Node, Object.assign({}, imageContainer), image),
            React.createElement(Node, Object.assign({}, detailsContainer),
                title,
                text,
                links)));
    }
};
export default ContentBlockView;
//# sourceMappingURL=content-block.view.js.map