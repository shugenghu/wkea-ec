import * as React from 'react';
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
const StoreSelectorView = props => {
    const { BodyContainerProps, FooterContainerProps, HeaderContainerProps, HeaderProps, ModuleProps, locationsMessage, search, state, spinner, locationsList, noLocationsMessage, terms } = props;
    return (React.createElement(Module, Object.assign({}, ModuleProps),
        React.createElement(Node, Object.assign({}, HeaderContainerProps), HeaderProps),
        React.createElement(Node, Object.assign({}, BodyContainerProps),
            locationsMessage,
            search,
            state.isSearchInProgress ?
                spinner :
                React.createElement(React.Fragment, null, locationsList ? locationsList : noLocationsMessage)),
        React.createElement(Node, Object.assign({}, FooterContainerProps), terms)));
};
export default StoreSelectorView;
//# sourceMappingURL=store-selector.view.js.map