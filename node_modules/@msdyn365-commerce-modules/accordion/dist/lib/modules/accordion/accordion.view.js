import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const accordionView = props => {
    const { heading, AccordionContainer, accordionItems } = props;
    if (!props.heading && !props.accordionButtons && !props.accordionItems) {
        props.context.telemetry.error('Accordion content is empty, module wont render');
        return null;
    }
    return (React.createElement(Module, Object.assign({}, AccordionContainer),
        React.createElement(Node, Object.assign({}, props.HeaderSection),
            React.createElement(Node, Object.assign({}, props.HeadingWrapper), heading),
            props.accordionButtons && (React.createElement(Node, Object.assign({}, props.accordianButtonsContainer), props.accordionButtons))),
        React.createElement(Node, Object.assign({}, props.accordionItemContainer), accordionItems)));
};
export default accordionView;
//# sourceMappingURL=accordion.view.js.map