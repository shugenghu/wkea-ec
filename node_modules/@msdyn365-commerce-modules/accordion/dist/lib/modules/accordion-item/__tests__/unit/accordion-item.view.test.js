import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import AccordionItemView from '../../accordion-item.view';
describe('video-player unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const myReactNodeChild1 = `whatever child module 1`;
        const myReactNodeChild2 = `whatever child module 2`;
        const accordionItemContent = [myReactNodeChild1, myReactNodeChild2];
        const mockProps = {
            ...moduleProps,
            className: 'ms-accordion-item-section',
            AccordionItemContainer: {
                moduleProps,
                className: 'ms-accordion-item'
            },
            drawer: { className: 'ms-accordion-header-section' },
            accordionSlots: {
                tag: 'div',
                className: 'ms-accordion-item-section',
                accordionItemContent: accordionItemContent
            },
            AccordionItemContent: { className: 'ms-accordion-item-content' },
        };
        const component = render(React.createElement(AccordionItemView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=accordion-item.view.test.js.map