import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import CarouselModule from '../carousel';
import CarouselView from '../carousel.view';
const mockData = {
    carouselId: 'carousel123'
};
const mockConfigWithFlipper = {
    hideFlipper: false,
    dismissEnabled: false
};
const mockResource = {
    flipperNext: 'next',
    flipperPrevious: 'prev',
    ariaLabelForSlide: 'Slide {0}',
    ariaLabelForDismissButton: 'cancel'
};
const mockActions = {};
describe('Carousel integration tests', () => {
    it('renders correctly', () => {
        const moduleProps = {
            ...buildMockModuleProps(mockData, mockActions, mockConfigWithFlipper), resources: mockResource,
            renderView: CarouselView
        };
        const component = render(React.createElement(CarouselModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=integration.test.js.map