import { buildMockCoreContext, buildMockModuleProps, buildMockRequest } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import CarouselView from '../../carousel.view';
const mockConfig = {
    className: 'carousel-mock',
    pauseOnHover: true,
    autoplay: true,
    transitionType: "slide"
};
const mockCoreContext = buildMockCoreContext({ app: { config: { hideRating: false } } });
const telemetryContent = { pageName: 'Homepage', moduleName: 'Add to Cart', telemetry: mockCoreContext.telemetry };
const mockRequest = buildMockRequest();
const mockContext = {
    request: mockRequest
};
describe('Carousel view tests', () => {
    it('render correctly', () => {
        const moduleProps = buildMockModuleProps({}, {}, mockConfig);
        const mockFunction = jest.fn();
        const mockProps = {
            ...moduleProps,
            carouselContainer: {
                moduleProps,
                className: 'ms-carousel'
            },
            context: mockContext,
            closeAnimation: '',
            next: mockFunction,
            previous: mockFunction,
            onIndicatorsClickHandler: mockFunction,
            handleDismissCarousel: mockFunction,
            handleOnExited: mockFunction,
            handleOnExiting: mockFunction,
            activeIndex: 0,
            closeNow: false,
            dismissCarousel: false,
            telemetryContent: telemetryContent
        };
        const view = render(React.createElement(CarouselView, Object.assign({}, mockProps)));
        expect(view);
    });
});
//# sourceMappingURL=carousel.view.test.js.map