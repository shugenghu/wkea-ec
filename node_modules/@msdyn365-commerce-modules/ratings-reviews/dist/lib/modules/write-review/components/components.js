import { getPayloadObject, getTelemetryAttributes, TelemetryConstant } from '@msdyn365-commerce-modules/utilities';
import React from 'react';
export const SignInMessage = (props) => {
    return React.createElement("p", { className: 'ms-write-review__sign-in' }, props.text);
};
export const SignInButton = (props) => {
    const { text, href, ariaLabel, telemetryContent } = props;
    const payLoad = getPayloadObject('click', telemetryContent, TelemetryConstant.SignIn);
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return React.createElement("a", Object.assign({ href: href, className: 'ms-write-review__sign-in-btn msc-btn', "aria-label": ariaLabel }, attributes), text);
};
export const ModalToggle = (props) => {
    const { text, onClick, ariaLabel, innerRef, telemetryContent, id } = props;
    const payLoad = getPayloadObject('click', telemetryContent, TelemetryConstant.WriteReview);
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return React.createElement("button", Object.assign({ id: id, className: 'ms-write-review__toggle msc-btn', "aria-label": ariaLabel, onClick: onClick, ref: innerRef }, attributes), text);
};
//# sourceMappingURL=components.js.map