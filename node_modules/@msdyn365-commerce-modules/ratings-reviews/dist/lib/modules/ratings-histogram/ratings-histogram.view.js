import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const RatingsHistogramView = props => {
    const { heading, histogramItems, histogramProps, moduleProps } = props;
    return (React.createElement(Module, Object.assign({}, moduleProps),
        heading,
        React.createElement(Node, Object.assign({}, histogramProps), histogramItems.map(item => {
            return histogramItem(item);
        }))));
};
const histogramItem = (props) => {
    return (React.createElement(Node, Object.assign({}, props.buttonProps),
        props.starLabel,
        props.bar,
        props.percentage));
};
export default RatingsHistogramView;
//# sourceMappingURL=ratings-histogram.view.js.map