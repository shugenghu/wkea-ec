import { render} from 'enzyme';
import * as React from 'react';
import {ModalToggle, SignInButton, SignInMessage } from '../components/components';

describe('write review components', () => {
    it('SignInMessage', () => {
        const component = render(<SignInMessage text='foo'/>);
        expect(component).toMatchSnapshot();
    });

    it('SignInButton', () => {
        const component = render(<SignInButton text='foo' href='url' ariaLabel='label'/>);
        expect(component).toMatchSnapshot();
    });

    it('ModalToggle', () => {
        const component = render(<ModalToggle text='foo' innerRef={undefined} ariaLabel='label' onClick={jest.fn()} id='foo' />);
        expect(component).toMatchSnapshot();
    });
});