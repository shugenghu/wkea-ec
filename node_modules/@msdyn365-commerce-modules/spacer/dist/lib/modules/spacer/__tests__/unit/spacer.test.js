import * as React from 'react';
import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import SpacerModule from '../../spacer';
import SpacerView from '../../spacer.view';
const mockConfig = {
    className: 'test-class'
};
describe('Spacer unit tests - Container', () => {
    it('renders correctly with text only', () => {
        const moduleProps = {
            ...buildMockModuleProps({}, {}, mockConfig),
            renderView: SpacerView
        };
        const module = render(React.createElement(SpacerModule, Object.assign({}, moduleProps)));
        expect(module).toMatchSnapshot();
    });
    it('renders correctly with no className', () => {
        const moduleProps = {
            ...buildMockModuleProps({}, {}, {}),
            renderView: SpacerView
        };
        const module = render(React.createElement(SpacerModule, Object.assign({}, moduleProps)));
        expect(module).toMatchSnapshot();
    });
});
//# sourceMappingURL=spacer.test.js.map