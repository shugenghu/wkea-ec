import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import SpacerModule from '../spacer';
import SpacerView from '../spacer.view';
const mockResources = {};
const mockConfig = {};
describe('Spacer integration tests', () => {
    it('renders correctly', () => {
        const moduleProps = {
            ...buildMockModuleProps({}, {}, mockConfig),
            resources: mockResources,
            renderView: SpacerView
        };
        const component = render(React.createElement(SpacerModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=integration.test.js.map