import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import SpacerView from '../../spacer.view';
describe('Spacer unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const mockProps = {
            ...moduleProps,
            text: '{TextComponent}',
            links: '{LinksComponent}',
            closeButton: '{CloseButonComponent}',
            className: 'custom-class',
            isVisible: true,
            onDismiss: jest.fn()
        };
        const component = render(React.createElement(SpacerView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=spacer.view.test.js.map