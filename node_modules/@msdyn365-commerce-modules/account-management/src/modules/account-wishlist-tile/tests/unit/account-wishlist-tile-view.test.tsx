/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
import { render } from 'enzyme';
import * as React from 'react';

import { buildMockModuleProps } from '@msdyn365-commerce/core';

import { IAccountWishlistTileData } from '../../account-wishlist-tile.data';
import { IAccountWishlistTileProps } from '../../account-wishlist-tile.props.autogenerated';
import AccountWishlistTileView from '../../account-wishlist-tile.view';

describe('Account-Wishlist-Tile unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps: IAccountWishlistTileProps<IAccountWishlistTileData> = buildMockModuleProps({}, {}) as IAccountWishlistTileProps<IAccountWishlistTileData>;

        const mockProps = {
            ...moduleProps,
            AccountWishlistTile: { moduleProps, className: 'module-class' },
            className: 'TestClassName',
            heading: '{HeadingComponent}',
            links: '{LinksComponent}',
            description: '{DescriptionComponent}'
        };
        const component = render(<AccountWishlistTileView {...mockProps} />);
        expect(component).toMatchSnapshot();
    });
});
