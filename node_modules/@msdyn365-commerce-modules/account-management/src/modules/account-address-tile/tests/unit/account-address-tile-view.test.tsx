/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
import { render } from 'enzyme';
import * as React from 'react';

import { buildMockModuleProps } from '@msdyn365-commerce/core';

import { IAccountAddressTileData } from '../../account-address-tile.data';
import { IAccountAddressTileProps } from '../../account-address-tile.props.autogenerated';
import AccountAddressTileView from '../../account-address-tile.view';

describe('Account-Address-Tile unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps: IAccountAddressTileProps<IAccountAddressTileData> = buildMockModuleProps({}, {}) as IAccountAddressTileProps<IAccountAddressTileData>;

        const mockProps = {
            ...moduleProps,
            AccountAddressTile: { moduleProps, className: 'module-class' },
            className: 'TestClassName',
            heading: '{HeadingComponent}',
            links: '{LinksComponent}',
            description: '{DescriptionComponent}'
        };
        const component = render(<AccountAddressTileView {...mockProps} />);
        expect(component).toMatchSnapshot();
    });
});
