/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
import { render } from 'enzyme';
import * as React from 'react';

import { buildMockModuleProps } from '@msdyn365-commerce/core';

import { IAccountGenericTileConfig, IAccountGenericTileProps } from '../../account-generic-tile.props.autogenerated';
import AccountGenericTileView from '../../account-generic-tile.view';

describe('Account-Generic-Tile unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps: IAccountGenericTileProps<IAccountGenericTileConfig> = buildMockModuleProps({}, {}) as IAccountGenericTileProps<IAccountGenericTileConfig>;

        const mockProps = {
            ...moduleProps,
            AccountGenericTile: { moduleProps, className: 'module-class' },
            className: 'TestClassName',
            heading: '{HeadingComponent}',
            links: '{LinksComponent}'
        };
        const component = render(<AccountGenericTileView {...mockProps} />);
        expect(component).toMatchSnapshot();
    });
});
