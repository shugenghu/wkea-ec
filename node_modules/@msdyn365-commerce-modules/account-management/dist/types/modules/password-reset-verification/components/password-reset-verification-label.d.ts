import * as React from 'react';
export interface IPasswordResetVerificationLabel {
    id: string;
    forId: string;
    className: string;
    text: string;
}
declare const PasswordResetVerificationLabel: React.FC<IPasswordResetVerificationLabel>;
export default PasswordResetVerificationLabel;
