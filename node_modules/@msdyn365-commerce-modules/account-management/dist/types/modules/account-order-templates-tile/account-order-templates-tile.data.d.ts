import { AsyncResult, Customer, FeatureState, ProductList } from '@msdyn365-commerce/retail-proxy';
export interface IAccountOrderTemplatesTileData {
    featureState: AsyncResult<FeatureState[]>;
    orderTemplates: AsyncResult<ProductList[]>;
    customerInformation: AsyncResult<Customer>;
}
