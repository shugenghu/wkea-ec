import * as React from 'react';
export interface IPasswordResetLabel {
    id: string;
    forId: string;
    className: string;
    text: string;
}
declare const PasswordResetLabel: React.FC<IPasswordResetLabel>;
export default PasswordResetLabel;
