import { shallow } from 'enzyme';
import * as React from 'react';
import DescriptionComponent from '../../components/account-profile-description';
describe('Account-Profile unit tests - Description', () => {
    it('renders correctly', () => {
        const mockProps = { className: 'className', description: 'Test string' };
        const component = shallow(React.createElement(DescriptionComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-profile-description.test.js.map