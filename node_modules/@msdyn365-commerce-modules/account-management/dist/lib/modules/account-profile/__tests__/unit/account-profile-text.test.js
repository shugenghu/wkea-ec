import { shallow } from 'enzyme';
import * as React from 'react';
import TextComponent from '../../components/account-profile-text';
describe('Account-profile unit tests - Text', () => {
    it('renders correctly', () => {
        const mockProps = { className: 'className', text: 'text' };
        const component = shallow(React.createElement(TextComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-profile-text.test.js.map