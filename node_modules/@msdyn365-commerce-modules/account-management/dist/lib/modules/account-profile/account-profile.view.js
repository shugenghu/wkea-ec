import * as React from 'react';
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
const AccountPreferencesSection = ({ accountPreferences, heading, personalization, extraPreferences }) => {
    return (React.createElement(Node, Object.assign({}, accountPreferences),
        heading,
        React.createElement(AccountPreferenceSection, Object.assign({}, personalization)),
        extraPreferences && extraPreferences.map((preferenceSecton, index) => {
            return React.createElement(AccountPreferenceSection, Object.assign({}, preferenceSecton, { key: index }));
        })));
};
const AccountPreferenceSection = ({ accountPreference, heading, description, buttonWrapper, buttonYesText, button, buttonNoText, }) => {
    return (React.createElement(Node, Object.assign({}, accountPreference),
        heading,
        description,
        React.createElement(Node, Object.assign({}, buttonWrapper),
            buttonNoText,
            button,
            buttonYesText)));
};
const AccountProfileSection = ({ AccountProfileItem, heading, links, description }) => {
    return (React.createElement(Node, Object.assign({}, AccountProfileItem),
        heading,
        description,
        links));
};
const AccountProfileView = props => {
    const { AccountProfile, accountProfileWrapper, heading, emailSection, nameSection, preferenceSection, customerAttributesWrapper, customerAttributesSection } = props;
    return (React.createElement(Module, Object.assign({}, AccountProfile),
        accountProfileWrapper && (React.createElement(Node, Object.assign({}, accountProfileWrapper),
            heading,
            React.createElement(AccountProfileSection, Object.assign({}, emailSection)),
            React.createElement(AccountProfileSection, Object.assign({}, nameSection)),
            React.createElement(AccountPreferencesSection, Object.assign({}, preferenceSection)))),
        customerAttributesWrapper && (React.createElement(Node, Object.assign({}, customerAttributesWrapper), customerAttributesSection))));
};
export default AccountProfileView;
//# sourceMappingURL=account-profile.view.js.map