import { buildMockModuleProps } from '@msdyn365-commerce/core-internal';
import * as React from 'react';
import * as renderer from 'react-test-renderer';
import OrganizationUsersTile from '../organization-users-tile';
const mockData = {
    users: {
        status: 'SUCCESS',
        result: 10
    }
};
const mockConfig = {
    heading: {
        text: 'Sample text',
    },
    links: [
        {
            linkText: 'view',
            linkUrl: {
                destinationUrl: '#'
            },
            ariaLabel: 'View details',
            openInNewTab: false
        }
    ],
    className: ' '
};
const mockActions = {};
const mockResources = {
    accountOrganizationUsersTileDescription: 'You have not added any organization user.'
};
describe('OrganizationUsersTile', () => {
    let moduleProps;
    beforeAll(() => {
        moduleProps = { ...buildMockModuleProps(mockData, mockActions, mockConfig), resources: mockResources };
    });
    it('renders correctly', () => {
        const component = renderer.create(React.createElement(OrganizationUsersTile, Object.assign({}, moduleProps)));
        const tree = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
});
//# sourceMappingURL=organization-users-tile.test.js.map