import { render } from 'enzyme';
import * as React from 'react';
import { buildMockModuleProps } from '@msdyn365-commerce/core';
import AccountCustomerBalanceTileView from '../../account-customer-balance-tile.view';
describe('Account-Customer-Balance-Tile View', () => {
    it('should render correctly', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const mockProps = {
            ...moduleProps,
            AccountCustomerBalanceTile: { moduleProps, className: 'module-class' },
            className: 'TestClassName',
            heading: '{HeadingComponent}',
            isContentDisplayed: true,
            content: 'test content',
            links: []
        };
        const component = render(React.createElement(AccountCustomerBalanceTileView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-customer-balance-tile-view.test.js.map