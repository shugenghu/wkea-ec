import * as React from 'react';
const GetMaxLength = (maxLength) => {
    if (maxLength) {
        const parsedMaxLength = parseInt(maxLength, 10);
        if (!isNaN(parsedMaxLength)) {
            return parsedMaxLength;
        }
    }
    return undefined;
};
const AccountProfileEditInput = ({ id, type, value, pattern, className, maxLength, onChange }) => (React.createElement("input", { id: id, type: type, value: value, className: `${className}-input ${className}-${id}`, pattern: pattern !== '' ? pattern : undefined, "aria-required": 'true', maxLength: GetMaxLength(maxLength), onChange: onChange }));
export default AccountProfileEditInput;
//# sourceMappingURL=account-profile-edit-input.js.map