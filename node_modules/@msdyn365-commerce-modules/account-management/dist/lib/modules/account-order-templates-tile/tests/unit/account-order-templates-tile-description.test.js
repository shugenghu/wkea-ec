import { shallow } from 'enzyme';
import * as React from 'react';
import { AccountOrderTemplatesTileDescription } from '../../components';
describe('Account-OrderTemplates-Tile unit tests - Description', () => {
    it('renders correctly', () => {
        const mockProps = { description: 'Test string' };
        const component = shallow(React.createElement(AccountOrderTemplatesTileDescription, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-order-templates-tile-description.test.js.map