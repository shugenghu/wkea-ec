import { render } from 'enzyme';
import * as React from 'react';
import { wrapInResolvedAsyncResult } from '@msdyn365-commerce-modules/retail-actions';
import { buildMockModuleProps } from '@msdyn365-commerce/core';
import AccountOrderTemplatesTileModule from '../account-order-templates-tile';
import AccountOrderTemplatesTileView from '../account-order-templates-tile.view';
const mockResources = {
    accountOrderTemplatesTileDescription: 'You have {0} order templates.'
};
const mockLink1 = {
    linkText: 'Mock link 1 title',
    linkUrl: {
        destinationUrl: 'Mock link 1 href'
    },
    ariaLabel: 'Mock link 1 alt text'
};
const mockHeading1 = {
    text: 'Mock heading 1',
    tag: "h1"
};
const mockConfigWithHeadingAndLink = {
    featureName: 'Dynamics.AX.Application.RetailB2BEcommerceFeature',
    className: 'testClassName',
    heading: mockHeading1,
    links: [mockLink1]
};
const mockConfigWithHeadingAndMultipleLink = {
    featureName: 'Dynamics.AX.Application.RetailB2BEcommerceFeature',
    className: 'testClassName',
    heading: mockHeading1,
    links: [mockLink1, mockLink1,]
};
const mockOrderTemplatesData = {
    customerInformation: wrapInResolvedAsyncResult({ IsB2b: true }),
    featureState: wrapInResolvedAsyncResult([{ Name: 'Dynamics.AX.Application.RetailB2BEcommerceFeature', IsEnabled: true }]),
    orderTemplates: wrapInResolvedAsyncResult([{}])
};
describe('Account-OrderTemplates-Tile integration tests', () => {
    it('renders correctly', () => {
        const moduleProps = {
            ...buildMockModuleProps(mockOrderTemplatesData, {}, mockConfigWithHeadingAndLink),
            resources: mockResources,
            renderView: AccountOrderTemplatesTileView
        };
        const component = render(React.createElement(AccountOrderTemplatesTileModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
    it('renders correctly with multiple links', () => {
        const moduleProps = {
            ...buildMockModuleProps(mockOrderTemplatesData, {}, mockConfigWithHeadingAndMultipleLink),
            resources: mockResources,
            renderView: AccountOrderTemplatesTileView
        };
        const component = render(React.createElement(AccountOrderTemplatesTileModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=integration.test.js.map