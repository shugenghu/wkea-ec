import { shallow } from 'enzyme';
import * as React from 'react';
import { wrapInResolvedAsyncResult } from '@msdyn365-commerce-modules/retail-actions';
import { buildMockRequest } from '@msdyn365-commerce/core';
import AccountOrderTemplatesTileModule from '../../account-order-templates-tile';
const mockResources = {
    accountAddressDescription: 'You have {0} address.'
};
const mockLink1 = {
    linkText: 'Mock link 1 title',
    linkUrl: {
        destinationUrl: 'Mock link 1 href'
    },
    ariaLabel: 'Mock link 1 alt text'
};
const mockHeading1 = {
    text: 'Mock heading 1',
    tag: "h1"
};
const mockConfigWithHeadingAndLink = {
    featureName: 'Dynamics.AX.Application.RetailB2BEcommerceFeature',
    className: 'testClassName',
    heading: mockHeading1,
    links: [mockLink1]
};
const mockConfigWithHeading = {
    featureName: 'Dynamics.AX.Application.RetailB2BEcommerceFeature',
    className: 'testClassName',
    heading: mockHeading1,
    links: []
};
const mockOrderTemplatesEmptyData = {
    customerInformation: wrapInResolvedAsyncResult({ IsB2b: true }),
    featureState: wrapInResolvedAsyncResult([{ Name: 'Dynamics.AX.Application.RetailB2BEcommerceFeature', IsEnabled: true }]),
    orderTemplates: wrapInResolvedAsyncResult([])
};
const mockOrderTemplatesData = {
    customerInformation: wrapInResolvedAsyncResult({ IsB2b: true }),
    featureState: wrapInResolvedAsyncResult([{ Name: 'Dynamics.AX.Application.RetailB2BEcommerceFeature', IsEnabled: true }]),
    orderTemplates: wrapInResolvedAsyncResult([{ ProductListLines: [{}] }])
};
const mockProps = {
    resources: mockResources,
    renderView: jest.fn(),
    renderModuleAttributes: jest.fn()
};
const mockRequest = buildMockRequest();
const mockContext = {
    request: mockRequest
};
describe('Account-OrderTemplates-Tile unit tests - Container', () => {
    it('renders correctly if all props is provided ', () => {
        const moduleProps = {
            config: mockConfigWithHeadingAndLink,
            data: mockOrderTemplatesData,
            context: mockContext,
            ...mockProps
        };
        shallow(React.createElement(AccountOrderTemplatesTileModule, Object.assign({}, moduleProps)));
        expect(moduleProps.renderView).toMatchSnapshot();
    });
    it('renders correctly if address has no records', () => {
        const moduleProps = {
            config: mockConfigWithHeadingAndLink,
            data: mockOrderTemplatesEmptyData,
            context: mockContext,
            ...mockProps
        };
        shallow(React.createElement(AccountOrderTemplatesTileModule, Object.assign({}, moduleProps)));
        expect(moduleProps.renderView).toMatchSnapshot();
    });
    it('renders correctly if links collection is empty', () => {
        const moduleProps = {
            config: mockConfigWithHeading,
            data: mockOrderTemplatesData,
            context: mockContext,
            ...mockProps
        };
        shallow(React.createElement(AccountOrderTemplatesTileModule, Object.assign({}, moduleProps)));
        expect(moduleProps.renderView).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-order-templates-tile.test.js.map