import { Module } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const AccountOrderTemplatesTileView = props => {
    const { AccountOrderTemplatesTile, heading, description, links } = props;
    return (React.createElement(Module, Object.assign({}, AccountOrderTemplatesTile),
        heading,
        description,
        links));
};
export default AccountOrderTemplatesTileView;
//# sourceMappingURL=account-order-templates-tile.view.js.map