import * as React from 'react';
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
const LoyaltyTermsView = props => {
    const { LoyaltyTerms, modalToggle, modal, loading } = props;
    return !loading && modal ? (React.createElement(Module, Object.assign({}, LoyaltyTerms),
        modalToggle,
        React.createElement(Node, Object.assign({}, modal.modal),
            React.createElement(Node, Object.assign({}, modal.modalHeader), modal.heading),
            React.createElement(Node, Object.assign({}, modal.modalBody),
                modal.terms,
                React.createElement(Node, Object.assign({}, modal.agreeSection),
                    modal.checkbox,
                    modal.agreeText)),
            React.createElement(Node, Object.assign({}, modal.modalFooter),
                modal.submitButton,
                modal.cancelButton)))) : null;
};
export default LoyaltyTermsView;
//# sourceMappingURL=loyalty-terms.view.js.map