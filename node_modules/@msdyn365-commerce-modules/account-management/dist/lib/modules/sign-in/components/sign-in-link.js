import { getPayloadObject, getTelemetryAttributes } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const SignInLink = ({ id, className, href, telemetryContent, ariaLabel, text }) => {
    const payLoad = getPayloadObject('click', telemetryContent, text);
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return (React.createElement("a", Object.assign({ id: id, className: className, href: href, "aria-label": ariaLabel }, attributes), text));
};
export default SignInLink;
//# sourceMappingURL=sign-in-link.js.map