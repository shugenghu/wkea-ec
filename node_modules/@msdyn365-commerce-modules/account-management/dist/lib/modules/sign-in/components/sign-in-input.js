import * as React from 'react';
const GetMaxLength = (maxLength) => {
    if (maxLength) {
        const parsedMaxLength = parseInt(maxLength, 10);
        if (!isNaN(parsedMaxLength)) {
            return parsedMaxLength;
        }
    }
    return undefined;
};
const SignInInput = ({ id, type, value, pattern, className, maxLength, ariaLabel, onChange, }) => (React.createElement("input", { id: id, type: type, value: value, className: `${className}-input ${className}-${id}`, pattern: pattern !== '' ? pattern : undefined, "aria-required": 'true', maxLength: GetMaxLength(maxLength), onChange: onChange, "aria-label": ariaLabel }));
export default SignInInput;
//# sourceMappingURL=sign-in-input.js.map