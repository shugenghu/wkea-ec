import * as React from 'react';
const SignInLabel = ({ id, forId, className, text }) => (React.createElement("label", { id: `${id}_label`, className: `${className}-label`, htmlFor: forId }, text));
export default SignInLabel;
//# sourceMappingURL=sign-in-label.js.map