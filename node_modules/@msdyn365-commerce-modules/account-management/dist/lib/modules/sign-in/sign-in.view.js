import * as React from 'react';
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
const LocalAccount = ({ localAccount, items, forgetPassword, disclaimer, error, signInButton }) => {
    return (React.createElement(Node, Object.assign({}, localAccount),
        items && items.map((item) => {
            return (React.createElement(Node, Object.assign({ key: item.key }, item.wrapper),
                item.label,
                item.error,
                item.input));
        }),
        forgetPassword,
        disclaimer,
        error,
        signInButton));
};
const SocialAccount = ({ socialAccounts, items, }) => {
    return (React.createElement(Node, Object.assign({}, socialAccounts), items && items.map((item) => {
        return (React.createElement(Node, Object.assign({ key: item.key }, item.button),
            item.image,
            item.text));
    })));
};
const SignInLoading = ({ modal, modalBody, icon, message, }) => {
    return (React.createElement(Node, Object.assign({}, modal),
        React.createElement(Node, Object.assign({}, modalBody),
            icon,
            message)));
};
const SignInView = props => {
    const { signIn, viewState, loading, defaultAADConainer, aadConainer, signInSection, signUpSection, signInSectionHeading, signInLocalAccount, signInSocialAccount, signUpSectionHeading, signUpDescription, signUpLink } = props;
    return (React.createElement(Module, Object.assign({}, signIn),
        viewState.isShowLoading && React.createElement(Node, Object.assign({}, defaultAADConainer)),
        viewState.isShowLoading && React.createElement(SignInLoading, Object.assign({}, loading)),
        React.createElement(Node, Object.assign({}, aadConainer),
            React.createElement(Node, Object.assign({}, signInSection),
                signInSectionHeading,
                LocalAccount(signInLocalAccount),
                SocialAccount(signInSocialAccount)),
            React.createElement(Node, Object.assign({}, signUpSection),
                signUpSectionHeading,
                signUpDescription,
                signUpLink))));
};
export default SignInView;
//# sourceMappingURL=sign-in.view.js.map