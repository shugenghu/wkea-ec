import { Button } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const SignUpButton = ({ id, className, text, ariaLabel, disabled, onClick }) => (React.createElement(Button, { id: id, className: className, "aria-label": ariaLabel, disabled: disabled, onClick: onClick }, text));
export default SignUpButton;
//# sourceMappingURL=sign-up-button.js.map