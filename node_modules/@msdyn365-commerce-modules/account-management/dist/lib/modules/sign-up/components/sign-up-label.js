import * as React from 'react';
const SignUpLabel = ({ id, forId, className, text }) => (React.createElement("label", { id: `${id}_label`, className: `${className}-label`, htmlFor: forId }, text));
export default SignUpLabel;
//# sourceMappingURL=sign-up-label.js.map