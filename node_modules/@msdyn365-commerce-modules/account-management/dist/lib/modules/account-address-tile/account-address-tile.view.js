import { Module } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const AccountAddressTileView = props => {
    const { AccountAddressTile, heading, description, links } = props;
    return (React.createElement(Module, Object.assign({}, AccountAddressTile),
        heading,
        description,
        links));
};
export default AccountAddressTileView;
//# sourceMappingURL=account-address-tile.view.js.map