import { render } from 'enzyme';
import * as React from 'react';
import { buildMockModuleProps } from '@msdyn365-commerce/core';
import AccountAddressTileView from '../../account-address-tile.view';
describe('Account-Address-Tile unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const mockProps = {
            ...moduleProps,
            AccountAddressTile: { moduleProps, className: 'module-class' },
            className: 'TestClassName',
            heading: '{HeadingComponent}',
            links: '{LinksComponent}',
            description: '{DescriptionComponent}'
        };
        const component = render(React.createElement(AccountAddressTileView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-address-tile-view.test.js.map