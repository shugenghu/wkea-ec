import { shallow } from 'enzyme';
import * as React from 'react';
import DescriptionComponent from '../../components/account-address-tile-description';
describe('Account-Address-Tile unit tests - Description', () => {
    it('renders correctly', () => {
        const mockProps = { description: 'Test string' };
        const component = shallow(React.createElement(DescriptionComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-address-tile-description.test.js.map