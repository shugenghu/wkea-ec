import { render } from 'enzyme';
import * as React from 'react';
import { wrapInResolvedAsyncResult } from '@msdyn365-commerce-modules/retail-actions';
import { buildMockModuleProps } from '@msdyn365-commerce/core';
import AccountAddressTileModule from '../account-address-tile';
import AccountAddressTileView from '../account-address-tile.view';
const mockResources = {
    accountAddressTileDescription: 'You have {0} address.'
};
const mockLink1 = {
    linkText: 'Mock link 1 title',
    linkUrl: {
        destinationUrl: 'Mock link 1 href'
    },
    ariaLabel: 'Mock link 1 alt text'
};
const mockHeading1 = {
    text: 'Mock heading 1',
    tag: "h1"
};
const mockConfigWithHeadingAndLink = {
    className: 'testClassName',
    heading: mockHeading1,
    links: [mockLink1]
};
const mockConfigWithHeadingAndMultipleLink = {
    className: 'testClassName',
    heading: mockHeading1,
    links: [mockLink1, mockLink1,]
};
const mockAddressData = {
    address: wrapInResolvedAsyncResult([{}])
};
describe('Account-Address-Tile integration tests', () => {
    it('renders correctly', () => {
        const moduleProps = {
            ...buildMockModuleProps(mockAddressData, {}, mockConfigWithHeadingAndLink),
            resources: mockResources,
            renderView: AccountAddressTileView
        };
        const component = render(React.createElement(AccountAddressTileModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
    it('renders correctly with multiple links', () => {
        const moduleProps = {
            ...buildMockModuleProps(mockAddressData, {}, mockConfigWithHeadingAndMultipleLink),
            resources: mockResources,
            renderView: AccountAddressTileView
        };
        const component = render(React.createElement(AccountAddressTileModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=integration.test.js.map