import { render } from 'enzyme';
import * as React from 'react';
import { buildMockModuleProps } from '@msdyn365-commerce/core';
import AccountWelcomeTileView from '../../account-welcome-tile.view';
describe('Account-Welcome-Tile unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const mockProps = {
            ...moduleProps,
            AccountWelcomeTile: { moduleProps, className: 'module-class' },
            className: 'TestClassName',
            heading: '{HeadingComponent}'
        };
        const component = render(React.createElement(AccountWelcomeTileView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-welcome-tile-view.test.js.map