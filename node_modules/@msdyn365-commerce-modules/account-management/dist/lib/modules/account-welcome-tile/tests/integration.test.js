import { render } from 'enzyme';
import * as React from 'react';
import { wrapInResolvedAsyncResult } from '@msdyn365-commerce-modules/retail-actions';
import { buildMockModuleProps } from '@msdyn365-commerce/core';
import AccountWelcomeTileModule from '../account-welcome-tile';
import AccountWelcomeTileView from '../account-welcome-tile.view';
const mockResources = {
    accountAddressTileDescription: 'You have {0} address.'
};
const mockHeading1 = {
    text: 'Mock heading 1',
    tag: "h1"
};
const mockConfigWithHeading = {
    className: 'testClassName',
    heading: mockHeading1
};
const mockAccountInformationData = {
    accountInformation: wrapInResolvedAsyncResult({
        AccountNumber: '1',
        FirstName: 'First Name'
    })
};
describe('Account-Welcome-Tile integration tests', () => {
    it('renders correctly', () => {
        const moduleProps = {
            ...buildMockModuleProps(mockAccountInformationData, {}, mockConfigWithHeading),
            resources: mockResources,
            renderView: AccountWelcomeTileView
        };
        const component = render(React.createElement(AccountWelcomeTileModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=integration.test.js.map