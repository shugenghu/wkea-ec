import { shallow } from 'enzyme';
import * as React from 'react';
import DescriptionComponent from '../../components/account-wishlist-tile-description';
describe('Account-Wishlist-Tile unit tests - Description', () => {
    it('renders correctly', () => {
        const mockProps = { description: 'Test string' };
        const component = shallow(React.createElement(DescriptionComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=account-wishlist-tile-description.test.js.map