import { Module } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const AccountWishlistTileView = props => {
    const { AccountWishlistTile, heading, description, links } = props;
    return (React.createElement(Module, Object.assign({}, AccountWishlistTile),
        heading,
        description,
        links));
};
export default AccountWishlistTileView;
//# sourceMappingURL=account-wishlist-tile.view.js.map