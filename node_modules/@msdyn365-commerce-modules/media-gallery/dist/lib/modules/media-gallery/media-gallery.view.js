import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const MediaGalleryView = props => {
    const { CarouselProps, Thumbnails, MediaGallery, Modal } = props;
    return (React.createElement(Module, Object.assign({}, MediaGallery),
        React.createElement(Node, Object.assign({}, CarouselProps)),
        Modal,
        _renderThumbnails(Thumbnails)));
};
const _renderThumbnails = (thumbnails) => {
    const { ThumbnailsContainerProps, SingleSlideCarouselComponentProps, items } = thumbnails;
    return (React.createElement(Node, Object.assign({}, ThumbnailsContainerProps),
        React.createElement(Node, Object.assign({}, SingleSlideCarouselComponentProps), items && items.map(_renderThumbnailItem))));
};
const _renderThumbnailItem = (thumbnail) => {
    const { ThumbnailItemContainerProps, Picture } = thumbnail;
    return (React.createElement(Node, Object.assign({}, ThumbnailItemContainerProps), Picture));
};
export default MediaGalleryView;
//# sourceMappingURL=media-gallery.view.js.map