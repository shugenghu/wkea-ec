import { shallow } from 'enzyme';
import * as React from 'react';
import WaitingComponent from '../../components/waiting';
describe('Checkout payment instrument unit tests - Waiting', () => {
    it('renders correctly', () => {
        const mockProps = {
            message: 'Loading...'
        };
        const component = shallow(React.createElement(WaitingComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=waiting.test.js.map