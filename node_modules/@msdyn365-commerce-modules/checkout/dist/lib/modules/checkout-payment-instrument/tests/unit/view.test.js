import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { shallow } from 'enzyme';
import * as React from 'react';
import ViewComponent from '../../checkout-payment-instrument.view';
describe('Checkout payment instrument unit tests - View', () => {
    const moduleProps = buildMockModuleProps({}, {});
    it('renders correctly', () => {
        const mockProps = {
            ...moduleProps,
            checkoutPaymentInstrument: { moduleProps, className: 'mock-module' },
            className: 'mock-class',
            waiting: '{waiting}',
            error: '{error}',
            paymentInformation: '{paymentInformation}',
            addPaymentForm: '{addPaymentForm}'
        };
        const component = shallow(React.createElement(ViewComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=view.test.js.map