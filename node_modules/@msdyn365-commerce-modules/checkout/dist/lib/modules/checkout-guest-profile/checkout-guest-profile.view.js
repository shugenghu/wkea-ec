import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const ShowContactInfo = ({ showInfoProps, email, editButton }) => (React.createElement(Node, Object.assign({}, showInfoProps),
    email,
    editButton));
export const AddContactInfo = ({ addFormProps, submitButton, cancelButton, inputGroupProps, label, error, input }) => (React.createElement(Node, Object.assign({}, addFormProps),
    React.createElement(Node, Object.assign({}, inputGroupProps),
        label,
        error,
        input),
    submitButton,
    cancelButton));
const CheckoutGuestProfileView = ({ moduleProps, showContactInfo, addContactInfo }) => {
    return (React.createElement(Module, Object.assign({}, moduleProps),
        showContactInfo && React.createElement(ShowContactInfo, Object.assign({}, showContactInfo)),
        addContactInfo && React.createElement(AddContactInfo, Object.assign({}, addContactInfo))));
};
export default CheckoutGuestProfileView;
//# sourceMappingURL=checkout-guest-profile.view.js.map