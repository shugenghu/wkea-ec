import { shallow } from 'enzyme';
import * as React from 'react';
import InputComponent from '../../components/checkout-loyalty-input';
describe('CheckoutLoyalty unit tests - Input', () => {
    it('renders correctly', () => {
        const mockProps = {
            className: 'test-class',
            type: 'number',
            value: '50',
            min: 0,
            max: 100,
            ariaValueMax: 0,
            ariaValueMin: 100,
            ariaValueNow: 50,
            id: 'foo',
            step: '0.1',
            onChange: jest.fn(),
            onBlur: jest.fn()
        };
        const component = shallow(React.createElement(InputComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=checkout-loyalty-input.test.js.map