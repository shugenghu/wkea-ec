import { shallow } from 'enzyme';
import * as React from 'react';
import LabelComponent from '../../components/checkout-loyalty-label';
describe('CheckoutLoyalty unit tests - Label', () => {
    it('renders correctly', () => {
        const mockProps = {
            className: 'test-class',
            text: 'text',
            htmlFor: 'foo'
        };
        const component = shallow(React.createElement(LabelComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=checkout-loyalty-label.test.js.map