import { shallow } from 'enzyme';
import * as React from 'react';
import TextComponent from '../../components/checkout-loyalty-text';
describe('CheckoutLoyalty unit tests - Text', () => {
    it('renders correctly', () => {
        const mockProps = {
            className: 'test-class',
            text: 'text'
        };
        const component = shallow(React.createElement(TextComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
    it('renders correctly without text', () => {
        const mockProps = {
            className: 'test-class'
        };
        const component = shallow(React.createElement(TextComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=checkout-loyalty-text.test.js.map