/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const Form = ({ formProps, label, inputPinLabel, inputExpLabel, alert, inputProps, inputNumProps, inputPinProps, inputExpProps, inputNumber, inputPin, inputExp, applyButton, supportExternalGiftCard, showGiftCardPinInput, showGiftCardExpInput, alertFieldLabel }) => (React.createElement(Node, Object.assign({}, formProps),
    alert,
    supportExternalGiftCard ?
        React.createElement(Node, Object.assign({}, inputProps),
            React.createElement(Node, Object.assign({}, inputNumProps),
                label,
                inputNumber),
            showGiftCardPinInput &&
                React.createElement(Node, Object.assign({}, inputPinProps),
                    inputPinLabel,
                    inputPin),
            showGiftCardExpInput &&
                React.createElement(Node, Object.assign({}, inputExpProps),
                    inputExpLabel,
                    inputExp),
            (showGiftCardPinInput || showGiftCardExpInput) && alertFieldLabel)
        : React.createElement(React.Fragment, null,
            label,
            inputNumber),
    applyButton));
export const Item = ({ itemProps, selectedGiftCard }) => (React.createElement(Node, Object.assign({}, itemProps), selectedGiftCard));
const GiftCardBalanceCheckView = props => {
    const { giftCardBalanceCheckProps, form, item } = props;
    return (React.createElement(Module, Object.assign({}, giftCardBalanceCheckProps),
        form && React.createElement(Form, Object.assign({}, form)),
        item && React.createElement(Item, Object.assign({}, item))));
};
export default GiftCardBalanceCheckView;
//# sourceMappingURL=gift-card-balance-check.view.js.map