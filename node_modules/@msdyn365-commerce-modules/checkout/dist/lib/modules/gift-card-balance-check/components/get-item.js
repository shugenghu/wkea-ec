import * as React from 'react';
export const getItem = ({ giftCard, getFormattedPrice, resources: { enteredGiftCardLabel, giftCardAvailableBalanceLabel } }) => {
    const balance = getFormattedPrice(giftCard.Balance, giftCard.CardCurrencyCode);
    const giftCardLast4Digit = (giftCard.Id || '').substr(-4);
    const itemProps = {
        className: 'ms-gift-card-balance-check__item'
    };
    const selectedGiftCard = (React.createElement("span", { className: 'ms-gift-card-balance-check__item-text' },
        enteredGiftCardLabel,
        " ",
        giftCardLast4Digit,
        " (",
        giftCardAvailableBalanceLabel,
        ' ',
        React.createElement("span", { className: 'ms-gift-card-balance-check__balance' }, balance),
        ")"));
    return {
        itemProps,
        id: giftCard && giftCard.Id,
        selectedGiftCard
    };
};
//# sourceMappingURL=get-item.js.map