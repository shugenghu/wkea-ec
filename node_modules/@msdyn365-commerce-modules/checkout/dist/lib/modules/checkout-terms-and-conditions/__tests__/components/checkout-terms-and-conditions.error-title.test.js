import { render } from 'enzyme';
import * as React from 'react';
import ErrorTitle from '../../components/checkout-terms-and-conditions.error-title';
describe('CheckoutTermsAndConditions - Error title', () => {
    it('renders correctly with error message', () => {
        const mockProps = {
            title: 'title'
        };
        const component = render(React.createElement(ErrorTitle, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
    it('renders correctly with error message not provided', () => {
        const mockProps = {};
        const component = render(React.createElement(ErrorTitle, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=checkout-terms-and-conditions.error-title.test.js.map