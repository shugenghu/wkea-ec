import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const TermsAndCondition = ({ termsAndConditionAcceptance, checkBox, description }) => {
    return (React.createElement(Node, Object.assign({}, termsAndConditionAcceptance),
        checkBox,
        description));
};
const ErrorMessage = ({ error, title, message, }) => {
    return (React.createElement(Node, Object.assign({}, error),
        title,
        message));
};
const CheckoutTermsAndConditionsView = props => {
    const { checkoutTermsAndConditions, viewState, termsAndConditionsContent, errorMessage, saveButton, editButton, cancelButton } = props;
    return (React.createElement(Module, Object.assign({}, checkoutTermsAndConditions),
        viewState.isError && errorMessage && React.createElement(ErrorMessage, Object.assign({}, errorMessage)),
        React.createElement(TermsAndCondition, Object.assign({}, termsAndConditionsContent)),
        viewState.isShowSaveButton && saveButton,
        viewState.isShowEditButton && editButton,
        viewState.isShowCancelButton && cancelButton));
};
export default CheckoutTermsAndConditionsView;
//# sourceMappingURL=checkout-terms-and-conditions.view.js.map