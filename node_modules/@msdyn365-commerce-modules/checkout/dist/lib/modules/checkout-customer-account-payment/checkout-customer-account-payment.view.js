/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const SummaryForm = ({ formProps, label, paymentAmount, addPaymentButton, appliedLine, bottomBorder }) => (React.createElement(Node, Object.assign({}, formProps),
    React.createElement(React.Fragment, null,
        label,
        paymentAmount,
        addPaymentButton,
        appliedLine,
        bottomBorder)));
export const EditForm = ({ formProps, inputLabel, inputAmount, addPaymentButton, customerName, customerAccountNumber, customerSince, accountCredit, alert, accountDetails, appliedLine, bottomBorder }) => (React.createElement(Node, Object.assign({}, formProps),
    React.createElement(React.Fragment, null,
        customerName,
        customerAccountNumber,
        customerSince,
        accountCredit,
        accountDetails,
        inputLabel,
        alert,
        inputAmount,
        addPaymentButton,
        appliedLine,
        bottomBorder)));
const CheckoutCustomerAccountView = props => {
    const { checkoutCustomerAccount, summaryView, editView, moduleState } = props;
    return (React.createElement(Module, Object.assign({}, checkoutCustomerAccount),
        moduleState.isReady && summaryView && React.createElement(SummaryForm, Object.assign({}, summaryView)),
        !moduleState.isReady && editView && React.createElement(EditForm, Object.assign({}, editView))));
};
export default CheckoutCustomerAccountView;
//# sourceMappingURL=checkout-customer-account-payment.view.js.map