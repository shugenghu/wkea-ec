import { getPayloadObject, getTelemetryAttributes, TelemetryConstant } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const CheckoutDeliveryOptionsRadioButton = ({ isChecked, value, ariaSetSize, ariaPosInSet, ariaLabel, telemetryContent, onChange }) => {
    if (!value) {
        return null;
    }
    const payLoad = getPayloadObject('click', telemetryContent, TelemetryConstant.DeliveryOptions);
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return (React.createElement("input", Object.assign({ className: 'ms-checkout-delivery-options__input-radio', checked: isChecked, "aria-checked": isChecked, type: 'radio', "aria-setsize": ariaSetSize, "aria-posinset": ariaPosInSet, "aria-label": ariaLabel, value: value, name: 'deliveryOptions' }, attributes, { onChange: onChange })));
};
export default CheckoutDeliveryOptionsRadioButton;
//# sourceMappingURL=checkout-delivery-options.radio-button.js.map