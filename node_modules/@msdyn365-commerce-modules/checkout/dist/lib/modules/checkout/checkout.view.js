import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const PickUpAtStoreComponent = ({ PickUpAtStore, label, location }) => (React.createElement(Node, Object.assign({}, PickUpAtStore),
    label,
    location));
export const LineItemComponent = ({ LineItem, item, pickUpAtStore }) => (React.createElement(Node, Object.assign({}, LineItem),
    item,
    pickUpAtStore && React.createElement(PickUpAtStoreComponent, Object.assign({}, pickUpAtStore))));
export const LineItemGroupComponent = ({ LineItemDeliveryGroup, LineItemList, heading, lineItems }) => (React.createElement(Node, Object.assign({}, LineItemDeliveryGroup),
    heading,
    React.createElement(Node, Object.assign({}, LineItemList), lineItems.map(lineItem => (React.createElement(LineItemComponent, Object.assign({ key: lineItem.LineId }, lineItem)))))));
export const LineItemsComponent = ({ LineItems, Header, heading, editLink, itemsForPickup, itemsForShip }) => (React.createElement(Node, Object.assign({}, LineItems),
    React.createElement(Node, Object.assign({}, Header),
        heading,
        editLink),
    itemsForPickup && React.createElement(LineItemGroupComponent, Object.assign({}, itemsForPickup)),
    itemsForShip && React.createElement(LineItemGroupComponent, Object.assign({}, itemsForShip))));
const OrderSummaryComponent = ({ heading, lines }) => (React.createElement("div", { className: 'msc-order-summary-wrapper' },
    heading,
    React.createElement("div", { className: 'msc-order-summary__items' }, lines && (React.createElement(React.Fragment, null,
        lines.subtotal,
        lines.shipping,
        lines.otherCharge,
        lines.tax,
        lines.totalDiscounts,
        lines.loyalty,
        lines.giftCard,
        lines.orderTotal)))));
const CheckoutView = props => {
    const { canShow, checkoutProps, headerProps, hasSalesOrder, bodyProps, mainProps, mainControlProps, sideProps, sideControlFirstProps, sideControlSecondProps, termsAndConditionsProps, orderConfirmation, loading, alert, title, guidedForm, orderSummary, lineItems, placeOrderButton, termsAndConditions, keepShoppingButton } = props;
    return (React.createElement(Module, Object.assign({}, checkoutProps),
        !hasSalesOrder && React.createElement(Node, Object.assign({}, headerProps), title),
        !hasSalesOrder &&
            React.createElement(Node, Object.assign({}, bodyProps),
                loading,
                alert,
                canShow && (React.createElement(React.Fragment, null,
                    React.createElement(Node, Object.assign({}, mainProps),
                        guidedForm,
                        React.createElement(Node, Object.assign({}, termsAndConditionsProps), termsAndConditions),
                        React.createElement(Node, Object.assign({}, mainControlProps),
                            placeOrderButton,
                            keepShoppingButton)),
                    React.createElement(Node, Object.assign({}, sideProps),
                        orderSummary && React.createElement(OrderSummaryComponent, Object.assign({}, orderSummary)),
                        React.createElement(Node, Object.assign({}, sideControlFirstProps),
                            React.createElement(Node, Object.assign({}, termsAndConditionsProps), termsAndConditions),
                            placeOrderButton,
                            keepShoppingButton),
                        lineItems && React.createElement(LineItemsComponent, Object.assign({}, lineItems)),
                        React.createElement(Node, Object.assign({}, sideControlSecondProps),
                            React.createElement(Node, Object.assign({}, termsAndConditionsProps), termsAndConditions),
                            placeOrderButton,
                            keepShoppingButton))))),
        hasSalesOrder && orderConfirmation));
};
export default CheckoutView;
//# sourceMappingURL=checkout.view.js.map