import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const From = ({ formProps, label, inputPinLabel, inputExpLabel, alert, inputProps, inputNumProps, inputPinProps, inputExpProps, inputNumber, inputPin, inputExp, applyButton, supportExternalGiftCard, showGiftCardPinInput, showGiftCardExpInput, alertFieldLabel }) => (React.createElement(Node, Object.assign({}, formProps),
    alert,
    supportExternalGiftCard ?
        React.createElement(Node, Object.assign({}, inputProps),
            React.createElement(Node, Object.assign({}, inputNumProps),
                label,
                inputNumber),
            showGiftCardPinInput &&
                React.createElement(Node, Object.assign({}, inputPinProps),
                    inputPinLabel,
                    inputPin),
            showGiftCardExpInput &&
                React.createElement(Node, Object.assign({}, inputExpProps),
                    inputExpLabel,
                    inputExp),
            (showGiftCardPinInput || showGiftCardExpInput) && alertFieldLabel)
        : React.createElement(React.Fragment, null,
            label,
            inputNumber),
    applyButton));
export const SelectedGiftCard = ({ itemProps, selectedGiftCard, removeButton }) => (React.createElement(Node, Object.assign({}, itemProps),
    selectedGiftCard,
    removeButton));
export const GiftCardList = ({ listProps, list }) => (React.createElement(Node, Object.assign({}, listProps), list.map(({ id, ...item }) => (React.createElement(SelectedGiftCard, Object.assign({ key: id }, item))))));
export const AddResource = ({ form, list }) => (React.createElement(React.Fragment, null,
    form && React.createElement(From, Object.assign({}, form)),
    list && React.createElement(GiftCardList, Object.assign({}, list))));
export const ShowResource = ({ title, list }) => (React.createElement(React.Fragment, null,
    title,
    list && React.createElement(GiftCardList, Object.assign({}, list))));
const CheckoutGiftCardView = props => {
    const { checkoutGiftCardProps, showGiftCard, addGiftCard } = props;
    return (React.createElement(Module, Object.assign({}, checkoutGiftCardProps),
        showGiftCard && React.createElement(ShowResource, Object.assign({}, showGiftCard)),
        addGiftCard && React.createElement(AddResource, Object.assign({}, addGiftCard))));
};
export default CheckoutGiftCardView;
//# sourceMappingURL=checkout-gift-card.view.js.map