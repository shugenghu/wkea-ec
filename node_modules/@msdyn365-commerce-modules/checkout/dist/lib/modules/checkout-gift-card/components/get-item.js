import { Button } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const getItem = ({ giftCard, getFormattedPrice, canRemove, onRemoveGiftCard, resources: { enteredGiftCardLabel, giftCardAvailableBalanceLabel, removeGiftCardButton } }) => {
    const balance = getFormattedPrice(giftCard.Balance, giftCard.CardCurrencyCode);
    const giftCardLast4Digit = (giftCard.Id || '').substr(-4);
    const onRemove = () => onRemoveGiftCard(giftCard.Id || '');
    const itemProps = {
        className: 'ms-checkout-gift-card__item'
    };
    const selectedGiftCard = (React.createElement("span", { className: 'ms-checkout-gift-card__item-text' },
        enteredGiftCardLabel,
        " ",
        giftCardLast4Digit,
        " (",
        giftCardAvailableBalanceLabel,
        ' ',
        React.createElement("span", { className: 'ms-checkout-gift-card__balance' }, balance),
        ")"));
    const removeButton = canRemove && (React.createElement(Button, { className: 'ms-checkout-gift-card__btn-remove', onClick: onRemove }, removeGiftCardButton));
    return {
        itemProps,
        id: giftCard.Id,
        selectedGiftCard,
        removeButton
    };
};
//# sourceMappingURL=get-item.js.map