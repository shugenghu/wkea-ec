import { getItem } from './get-item';
export const getList = ({ giftCards, canRemove, resources, getFormattedPrice, onRemoveGiftCard }) => {
    if (!giftCards || !giftCards.length) {
        return undefined;
    }
    const listProps = {
        className: 'ms-checkout-gift-card__list'
    };
    const list = giftCards.map(giftCard => getItem({
        canRemove,
        getFormattedPrice,
        giftCard,
        onRemoveGiftCard,
        resources
    }));
    return {
        listProps,
        list
    };
};
//# sourceMappingURL=get-list.js.map