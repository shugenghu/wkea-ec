import { INodeProps } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export interface IAddContactInfoInput {
    inputRef?: React.RefObject<HTMLInputElement>;
    hasError: boolean;
    email: string;
    resources: {
        emailLabel: string;
        emailErrortext: string;
        saveBtnLabel: string;
        cancelBtnLabel: string;
    };
    canSubmit: boolean;
    canCancel: boolean;
    onChange(email: string): void;
    onSubmit(): void;
    onCancel(): void;
}
export interface IAddContactInfo {
    addFormProps: INodeProps;
    submitButton: React.ReactNode;
    cancelButton: React.ReactNode;
    inputGroupProps: INodeProps;
    label: React.ReactNode;
    error: React.ReactNode;
    input: React.ReactNode;
}
declare const getAddContactInfo: ({ hasError, inputRef, email, canSubmit, canCancel, onChange, onSubmit, onCancel, resources: { emailLabel, emailErrortext, saveBtnLabel, cancelBtnLabel } }: IAddContactInfoInput) => IAddContactInfo;
export default getAddContactInfo;
