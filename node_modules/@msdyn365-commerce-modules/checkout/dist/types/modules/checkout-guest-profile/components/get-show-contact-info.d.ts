import * as React from 'react';
import { INodeProps } from '@msdyn365-commerce-modules/utilities';
export interface IShowContactInfoInput {
    canEdit: boolean;
    email: string;
    resources: {
        editBtnLabel: string;
    };
    onEdit(): void;
}
export interface IShowContactInfo {
    showInfoProps: INodeProps;
    email: React.ReactNode;
    editButton?: React.ReactNode;
}
declare const getContactInfo: ({ canEdit, email, resources: { editBtnLabel }, onEdit }: IShowContactInfoInput) => IShowContactInfo;
export default getContactInfo;
