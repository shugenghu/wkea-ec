/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/

// tslint:disable-next-line:no-unused-variable
import { shallow } from 'enzyme';
import * as React from 'react';
import WaitingComponent, { IPaymentWaitingProps } from '../../components/waiting';

describe('Checkout payment instrument unit tests - Waiting', () => {
    it('renders correctly', () => {
        const mockProps: IPaymentWaitingProps = {
            message: 'Loading...'
        };
        const component = shallow(<WaitingComponent {...mockProps} />);
        expect(component).toMatchSnapshot();
    });
});
