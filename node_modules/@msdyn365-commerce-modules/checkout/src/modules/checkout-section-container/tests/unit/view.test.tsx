/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/

import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { ICheckoutSectionContainerViewProps } from '../../checkout-section-container';
import { ICheckoutSectionContainerProps } from '../../checkout-section-container.props.autogenerated';
import ViewComponent from '../../checkout-section-container.view';

const moduleProps: ICheckoutSectionContainerProps<{}> = buildMockModuleProps({}, {}) as ICheckoutSectionContainerProps<{}>;
const mockProps: ICheckoutSectionContainerViewProps = {
    ...moduleProps,
    className: 'mock class',
    checkoutPlainContainerProps: { moduleProps, className: 'module-class-checkoutPlainContainerProps' },
    items: [
        {
            id: '1',
            loading: '{loading}',
            itemProps: { className: 'node-class-itemProps' },
            item: '{item1}'
        },
        {
            id: '2',
            loading: false,
            itemProps: { className: 'node-class-itemProps' },
            item: '{item2}'
        }
    ]
};

describe('Checkout unit tests - View', () => {
    it('renders correctly', () => {
        const component = render(<ViewComponent {...mockProps} />);
        expect(component).toMatchSnapshot();
    });
});
