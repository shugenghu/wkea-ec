
/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
// tslint:disable-next-line:no-unused-variable
import { render } from 'enzyme';
import * as React from 'react';
import ErrorTitle from '../../components/checkout-terms-and-conditions.error-title';

describe('CheckoutTermsAndConditions - Error title', () => {
    it('renders correctly with error message', () => {
        const mockProps = {
            title: 'title'
        };
        const component = render(<ErrorTitle {...mockProps} />);
        expect(component).toMatchSnapshot();
    });

    it('renders correctly with error message not provided', () => {
        const mockProps = {
        };
        const component = render(<ErrorTitle {...mockProps} />);
        expect(component).toMatchSnapshot();
    });
});
