import 'jest';

import { SearchState } from '../search-state';
import { createSearchStateInput } from '../search-state-data-action';

describe('createSearchStateInput', () => {
    it('Properly creates input', () => {
        const state: SearchState = { searchText: 'test' };

        const input = createSearchStateInput(state);

        expect(input.result).toBe(state);
    });
});
