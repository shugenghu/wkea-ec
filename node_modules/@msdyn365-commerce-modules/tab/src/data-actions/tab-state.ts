/**
 * State information related to handle Tab scenarios
 */
export interface IActiveTabState {
    /**
     * Set this to index of the active tab-item to communicate that only the
     * active tab-item's content should be visible
     */
    activeContentIndex?: number;
    tabId?: string;
}
