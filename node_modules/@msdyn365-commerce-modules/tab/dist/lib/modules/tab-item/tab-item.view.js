import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
import { TabItemType } from './tab-item';
const TabItemView = props => {
    const { tabItemType, tabSlots, TabContentContainer, TabHeaderContainer, TabItemContent, tabHeader } = props;
    switch (tabItemType) {
        case TabItemType.TabContent:
            return (React.createElement(Module, Object.assign({}, TabContentContainer),
                React.createElement(Node, Object.assign({}, TabItemContent), tabSlots && tabSlots.tabItemContent && tabSlots.tabItemContent.length && (_renderTabItemContent(tabSlots.tabItemContent)))));
        case TabItemType.TabHeader:
            return (React.createElement(Module, Object.assign({}, TabHeaderContainer), tabHeader));
        default:
            return null;
    }
};
const _renderTabItemContent = (items) => {
    return (React.createElement(React.Fragment, null, items && items.length ? (items.map((item, index) => {
        return (React.createElement(React.Fragment, { key: index }, item));
    })) : null));
};
export default TabItemView;
//# sourceMappingURL=tab-item.view.js.map