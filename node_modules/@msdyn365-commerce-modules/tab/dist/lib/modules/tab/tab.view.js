import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const tabView = props => {
    const { heading, TabContainer, tabHeaders, tabContents } = props;
    if (!props.heading && !props.tabHeaders && !props.tabContents) {
        props.context.telemetry.error('Tab content is empty, module wont render');
        return null;
    }
    return (React.createElement(Module, Object.assign({}, TabContainer),
        React.createElement(Node, Object.assign({}, props.headerSection), heading),
        React.createElement(Node, Object.assign({}, props.tabsSection), tabHeaders),
        React.createElement(Node, Object.assign({}, props.tabItemContainer), tabContents)));
};
export default tabView;
//# sourceMappingURL=tab.view.js.map