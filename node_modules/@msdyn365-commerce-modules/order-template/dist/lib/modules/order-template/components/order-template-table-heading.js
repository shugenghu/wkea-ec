import * as React from 'react';
export class OrderTemplateTableHeading extends React.PureComponent {
    constructor() {
        super(...arguments);
        this.state = {
            isChecked: false
        };
        this.getOnChange = () => (event) => {
            this.setState({
                isChecked: !this.state.isChecked
            }, () => {
                this.props.clickHandler(this.state.isChecked);
            });
        };
    }
    render() {
        return (React.createElement("thead", null,
            React.createElement("tr", { className: 'ms-order-template-table-line__heading' },
                React.createElement("th", null,
                    React.createElement("input", { type: 'checkbox', className: 'bulk-selection-all', "aria-checked": this.state.isChecked, onChange: this.getOnChange() })),
                React.createElement("th", null),
                React.createElement("th", null, this.props.productNumber),
                React.createElement("th", null, this.props.productName),
                React.createElement("th", null, this.props.productUnitPrice),
                React.createElement("th", null, this.props.productUOM),
                React.createElement("th", null, this.props.productQuantity),
                React.createElement("th", null, this.props.productActions))));
    }
}
//# sourceMappingURL=order-template-table-heading.js.map