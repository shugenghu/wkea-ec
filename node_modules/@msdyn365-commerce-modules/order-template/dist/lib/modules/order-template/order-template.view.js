import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const OrderTemplateLinesView = (props) => {
    const { OrderTemplateLines, status, statusMessage, heading, ProductsTable, ProductsTableHeading, orderLines, ProductsTableRow, productsStatusMessage, OrderTemplateActions, orderActions, pagination, itemAddedToCartDialog, bulkOrderLineActions } = props;
    return (React.createElement(Module, Object.assign({}, OrderTemplateLines),
        heading,
        OrderTemplateActions && (React.createElement(Node, Object.assign({ className: OrderTemplateActions.className }, OrderTemplateActions),
            orderActions?.addTemplateToBag,
            orderActions?.renameTemplate,
            orderActions?.removeTemplate,
            productsStatusMessage)),
        bulkOrderLineActions,
        status !== 'SUCCESS' && statusMessage,
        ProductsTable && orderLines && orderLines.length > 0 && (React.createElement(Node, { className: 'ms-order-template__content' },
            React.createElement(Node, Object.assign({ className: ProductsTable.className }, ProductsTable),
                ProductsTableHeading,
                React.createElement("tbody", null, orderLines &&
                    orderLines.map(product => {
                        return _renderItem(product, ProductsTableRow);
                    }))),
            pagination)),
        itemAddedToCartDialog));
};
const _renderItem = (product, ProductsTableRow) => {
    const { key, selectLine, productImage, productPrice, productNumber, addToBagButton, removeButton, productInfo, productUnitOfMeasure, productQuantityView } = product;
    return (React.createElement(Node, Object.assign({ key: key, className: ProductsTableRow.className }, ProductsTableRow),
        React.createElement("td", null, selectLine),
        React.createElement("td", null, productImage),
        React.createElement("td", null, productNumber),
        React.createElement("td", null, productInfo),
        React.createElement("td", null, productPrice),
        React.createElement("td", null, productUnitOfMeasure),
        React.createElement("td", null, productQuantityView),
        React.createElement("td", null,
            addToBagButton,
            removeButton)));
};
export default OrderTemplateLinesView;
//# sourceMappingURL=order-template.view.js.map