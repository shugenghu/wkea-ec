/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import * as React from 'react';
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
export const OrderTemplateListView = props => {
    const { OrderTemplateList, status, statusMessage, heading, OrderTemplates, orderTemplates, OrderTemplate, createOrderTemplateButton, templateHeaderStatusMessage } = props;
    return (React.createElement(Module, Object.assign({}, OrderTemplateList),
        heading,
        status !== 'SUCCESS' && statusMessage,
        React.createElement(Node, { className: '' },
            createOrderTemplateButton,
            templateHeaderStatusMessage),
        OrderTemplates && orderTemplates && orderTemplates.length > 0 &&
            React.createElement(Node, Object.assign({ className: OrderTemplates.className }, OrderTemplates), orderTemplates && orderTemplates.map((orderTemplate) => {
                return _renderOrderTemplate(orderTemplate, OrderTemplate);
            }))));
};
const _renderOrderTemplate = (orderTemplate, OrderTemplate) => {
    const { key, addToCartButton, orderTemplateImage, orderTemplateStatusMessage, removeButton, orderTemplateName } = orderTemplate;
    return (React.createElement(Node, Object.assign({ key: key, className: OrderTemplate.className }, OrderTemplate),
        orderTemplateName,
        orderTemplateImage,
        addToCartButton,
        removeButton,
        orderTemplateStatusMessage));
};
export default OrderTemplateListView;
//# sourceMappingURL=order-template-list.view.js.map