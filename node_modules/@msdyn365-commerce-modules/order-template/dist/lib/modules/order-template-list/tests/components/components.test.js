import { render } from 'enzyme';
import * as React from 'react';
import { OrderTemplateListStatusMessage } from '../../components';
describe('OrderTemplateList- Unit Tests', () => {
    it('OrderTemplateListStatusMessage - LOADING - renders correctly', () => {
        const mockPropsMessage = {
            errorType: 'LOADING',
            text: 'Loading...'
        };
        const component = render(React.createElement(OrderTemplateListStatusMessage, Object.assign({}, mockPropsMessage)));
        expect(component).toMatchSnapshot();
    });
    it('OrderTemplateListStatusMessage - EMPTY - renders correctly', () => {
        const mockPropsMessage = {
            errorType: 'EMPTY',
            text: 'Your order template lines is empty.'
        };
        const component = render(React.createElement(OrderTemplateListStatusMessage, Object.assign({}, mockPropsMessage)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=components.test.js.map