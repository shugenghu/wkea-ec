import { buildMockModuleProps, buildMockRequest } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { TextBlock, TextBlockView } from '../../..';
const mockConfig = {
    paragraph: '<p> This is a sample text block</p>'
};
const mockRequest = buildMockRequest();
const mockContext = {
    request: mockRequest
};
describe('TextBlock', () => {
    it('renders correctly', () => {
        const moduleProps = {
            ...buildMockModuleProps({}, {}, mockConfig, mockContext),
            renderView: TextBlockView
        };
        const component = render(React.createElement(TextBlock, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=text-block.test.js.map