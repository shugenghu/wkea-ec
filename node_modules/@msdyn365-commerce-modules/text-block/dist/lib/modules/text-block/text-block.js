import * as MsDyn365 from '@msdyn365-commerce/core';
import classnames from 'classnames';
import * as React from 'react';
class TextBlock extends React.PureComponent {
    constructor() {
        super(...arguments);
        this.handleTextChange = (event) => this.props.config.paragraph = event.target.value;
    }
    render() {
        const { paragraph, className } = this.props.config;
        const text = paragraph && React.createElement(MsDyn365.RichTextComponent, { text: paragraph, editProps: { onEdit: this.handleTextChange, requestContext: this.props.context.request } });
        const textBlockViewProps = {
            ...this.props,
            paragraph: text,
            className: classnames('ms-text-block', className)
        };
        return this.props.renderView(textBlockViewProps);
    }
}
export default TextBlock;
//# sourceMappingURL=text-block.js.map