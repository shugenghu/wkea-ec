import * as React from 'react';
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
const AddressAddUpdate = ({ AddressForm, heading, items, hasError, error, isShowSaveButton, saveButton, isShowCancelButton, cancelButton, }) => {
    return (React.createElement(Node, Object.assign({}, AddressForm),
        heading,
        items && items.map((item) => {
            const { AddressItem, key, label, alert, input } = item;
            return (React.createElement(Node, Object.assign({}, AddressItem, { key: key }),
                label,
                hasError && alert,
                input));
        }),
        hasError &&
            React.createElement(Node, Object.assign({}, error.AddressError),
                error.title,
                error.message),
        isShowSaveButton && saveButton,
        isShowCancelButton && cancelButton));
};
const BusinessAccountAddressView = props => {
    const { BusinessAccountAddress, showAddOrUpdateAddress } = props;
    return (React.createElement(Module, Object.assign({}, BusinessAccountAddress), React.createElement(AddressAddUpdate, Object.assign({}, showAddOrUpdateAddress))));
};
export default BusinessAccountAddressView;
//# sourceMappingURL=business-account-address.view.js.map