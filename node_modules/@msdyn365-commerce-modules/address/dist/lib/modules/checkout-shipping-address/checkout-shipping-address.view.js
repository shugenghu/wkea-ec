import * as React from 'react';
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
const AddressShow = ({ AddressDetail, items }) => {
    return (React.createElement(Node, Object.assign({}, AddressDetail), items && items.map((item) => {
        return (React.createElement(React.Fragment, null, item.description));
    })));
};
const AddressSelect = ({ SelectAddress, addButton, items, isShowSaveButton, saveButton, isShowCancelButton, cancelButton, }) => {
    return (React.createElement(Node, Object.assign({}, SelectAddress),
        addButton,
        items && items.map((item) => {
            const SelectItem = item.SelectItem;
            return (React.createElement(Node, Object.assign({}, SelectItem, { key: item.key }),
                item.input,
                React.createElement(AddressShow, Object.assign({}, item.showItems))));
        }),
        isShowSaveButton && saveButton,
        isShowCancelButton && cancelButton));
};
const AddressAddUpdate = ({ AddressForm, heading, items, hasError, error, isShowSaveButton, saveButton, isShowCancelButton, cancelButton, }) => {
    return (React.createElement(Node, Object.assign({}, AddressForm),
        heading,
        items && items.map((item) => {
            const { AddressItem, key, label, alert, input } = item;
            return (React.createElement(Node, Object.assign({}, AddressItem, { key: key }),
                label,
                alert,
                input));
        }),
        hasError && React.createElement(Node, Object.assign({}, error.AddressError),
            error.title,
            error.message),
        isShowSaveButton && saveButton,
        isShowCancelButton && cancelButton));
};
const CheckoutShippingAddressView = props => {
    const { CheckoutShippingAddress, viewState, showAddress, showAddressSelect, showAddOrUpdateAddress } = props;
    return (React.createElement(Module, Object.assign({}, CheckoutShippingAddress),
        viewState.isShowAddress && React.createElement(AddressShow, Object.assign({}, showAddress)),
        viewState.isShowAddresList && React.createElement(AddressSelect, Object.assign({}, showAddressSelect)),
        viewState.isShowAddOrUpdateAddress && React.createElement(AddressAddUpdate, Object.assign({}, showAddOrUpdateAddress))));
};
export default CheckoutShippingAddressView;
//# sourceMappingURL=checkout-shipping-address.view.js.map