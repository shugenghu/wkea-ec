import * as React from 'react';
import { AddressItemType } from '../../common/address-format.data';
import AddressDetailItemComponent from './address-detail-item';
export const AddressShow = (props) => {
    const { addressFormat, addressPurposes, address } = props;
    return {
        AddressDetail: { className: 'msc-address-detail' },
        items: addressFormat.map((item) => {
            if (item.name === AddressItemType[AddressItemType.AddressTypeValue]) {
                const addressType = addressPurposes.find((addressTypeValue) => addressTypeValue.AddressType.toString() === (address[item.name] || '').toString());
                if (addressType) {
                    const value = addressType.Name || '';
                    return {
                        key: item.name,
                        description: React.createElement(AddressDetailItemComponent, Object.assign({}, { isNewLine: item.isNewLine, isShowLabel: item.type === AddressItemType.Phone, labelText: item.label, name: item.name, value: value }))
                    };
                }
            }
            return {
                key: item.name,
                description: React.createElement(AddressDetailItemComponent, Object.assign({}, { isNewLine: item.isNewLine, isShowLabel: item.type === AddressItemType.Phone, labelText: item.label, name: item.name, value: (address[item.name] || '').toString() }))
            };
        })
    };
};
//# sourceMappingURL=address-show.js.map