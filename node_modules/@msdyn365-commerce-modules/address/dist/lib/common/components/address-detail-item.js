import classname from 'classnames';
import * as React from 'react';
const AddressDetailItem = ({ isNewLine, isShowLabel, labelText, name, value }) => {
    const className = `msc-address-detail__item msc-address-detail__item-${name.toLowerCase()}`;
    const isEmpty = !(value && value.length > 0);
    const mainClass = isShowLabel ? `msc-address-detail__item msc-address-detail__item-address-detail_${labelText}` : classname(className, { 'msc-address-detail__item-empty': isEmpty, 'msc-address-detail__item-newline': isNewLine });
    return (React.createElement("span", { className: mainClass },
        isShowLabel && React.createElement(React.Fragment, null,
            React.createElement("span", { className: `${className}-label` }, labelText),
            React.createElement("span", { className: classname(className, { 'msc-address-detail__item-empty': isEmpty, 'msc-address-detail__item-newline': isNewLine }), "aria-hidden": true })),
        value));
};
export default AddressDetailItem;
//# sourceMappingURL=address-detail-item.js.map