import { getPayloadObject, getTelemetryAttributes } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const AdressInput = ({ id, name, className, type, value, maxLength, additionalAddributes, autoFocus, telemetryContent, onChange }) => {
    const payLoad = getPayloadObject('click', telemetryContent, name);
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return (React.createElement("input", Object.assign({ name: name, id: id, className: `${className}__input ${className}__input-${type}`, type: type, autoFocus: autoFocus, value: value, maxLength: maxLength }, (additionalAddributes || {}), attributes, { onChange: onChange })));
};
export default AdressInput;
//# sourceMappingURL=address-input.js.map