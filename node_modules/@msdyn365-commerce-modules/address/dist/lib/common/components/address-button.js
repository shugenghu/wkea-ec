import { getPayloadObject, getTelemetryAttributes } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const AddressButton = ({ className, text, ariaLabel, disabled, telemetryContent, onClick }) => {
    const payLoad = getPayloadObject('click', telemetryContent, text);
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return (React.createElement("button", Object.assign({ className: className, "aria-label": ariaLabel, disabled: disabled, onClick: onClick }, attributes), text));
};
export default AddressButton;
//# sourceMappingURL=address-button.js.map