import { getPayloadObject, getTelemetryAttributes } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const AdressCheckbox = ({ id, name, className, type, isChecked, additionalAddributes, telemetryContent, autoFocus, onChange }) => {
    const payLoad = getPayloadObject('click', telemetryContent, name);
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return (React.createElement("input", Object.assign({ name: name, id: id, className: `${className}__input ${className}__input-${type}`, type: 'checkbox', autoFocus: autoFocus, "aria-checked": isChecked, onChange: onChange, checked: isChecked }, (additionalAddributes || {}), attributes)));
};
export default AdressCheckbox;
//# sourceMappingURL=address-checkbox.js.map