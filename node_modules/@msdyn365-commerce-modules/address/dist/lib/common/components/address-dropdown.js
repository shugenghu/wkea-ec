import * as React from 'react';
const getDropdownItem = (key, value, selectedValue) => {
    let isSelected;
    if (typeof (key) === 'number') {
        isSelected = key === selectedValue;
    }
    else {
        isSelected = typeof (selectedValue) === 'string' && (key || '').toLowerCase() === (selectedValue || '').toLowerCase();
    }
    return (React.createElement("option", { key: key, value: key, "aria-selected": isSelected }, value));
};
const AdressDropdown = ({ id, name, className, value, additionalAddributes, displayData, onChange }) => (React.createElement("select", Object.assign({ id: id, className: `${className}__dropdown`, name: name, value: value, onChange: onChange }, (additionalAddributes || {})), displayData && displayData.map(item => (getDropdownItem(item.key, item.value, value)))));
export default AdressDropdown;
//# sourceMappingURL=address-dropdown.js.map