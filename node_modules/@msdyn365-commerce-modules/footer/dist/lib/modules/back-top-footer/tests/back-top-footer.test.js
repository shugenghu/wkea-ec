import { buildMockModuleProps } from '@msdyn365-commerce/core';
import * as React from 'react';
import * as renderer from 'react-test-renderer';
import BackTopFooter from '../back-top-footer';
const mockData = {
    myText: { text: 'simple text' }
};
const mockConfig = {
    ariaLabel: 'back to top',
    backtoTopText: 'top',
    destination: '#renderPage'
};
const mockConfigWithoutDestination = {
    ariaLabel: 'back to top',
    backtoTopText: 'top',
};
const mockActions = {};
describe('BackTopFooter', () => {
    let moduleProps;
    beforeAll(() => {
        moduleProps = buildMockModuleProps(mockData, mockActions, mockConfig);
    });
    it('renders correctly', () => {
        const component = renderer.create(React.createElement(BackTopFooter, Object.assign({}, moduleProps)));
        const tree = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
    it('renders correctly without destination', () => {
        const modulePropsWithoutDest = buildMockModuleProps(mockData, mockActions, mockConfigWithoutDestination);
        const component = renderer.create(React.createElement(BackTopFooter, Object.assign({}, modulePropsWithoutDest)));
        const tree = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
});
//# sourceMappingURL=back-top-footer.test.js.map