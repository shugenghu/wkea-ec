import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import React from 'react';
export const FooterCategoryView = props => {
    const { categories, FooterList, FooterListItem } = props;
    if (!categories || !categories.length) {
        props.context.telemetry.error('Footer content is empty, module wont render');
        return null;
    }
    return (React.createElement(Module, Object.assign({}, FooterList), categories.map((category, index) => {
        return (React.createElement(Node, Object.assign({}, FooterListItem, { key: index }), category));
    })));
};
export default FooterCategoryView;
//# sourceMappingURL=footer-category.view.js.map