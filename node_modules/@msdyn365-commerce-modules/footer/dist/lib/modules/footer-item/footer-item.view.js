import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export class FooterItemView extends React.PureComponent {
    render() {
        const { FooterItem } = this.props;
        return (React.createElement(Module, Object.assign({}, FooterItem), this._renderElement(this.props)));
    }
    _renderElement(props) {
        const { heading, HeadingContainer, Link, linkText, image, text, EditableLink } = props;
        if (heading) {
            return (React.createElement(Node, Object.assign({}, HeadingContainer),
                " ",
                heading,
                " "));
        }
        else if (Link && image) {
            return (React.createElement(Node, Object.assign({}, Link),
                linkText,
                image));
        }
        else if (Link) {
            return (EditableLink);
        }
        return (React.createElement(React.Fragment, null,
            text,
            image));
    }
}
export default FooterItemView;
//# sourceMappingURL=footer-item.view.js.map