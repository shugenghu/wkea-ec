import { buildMockModuleProps, buildMockRequest } from '@msdyn365-commerce/core';
import * as React from 'react';
import * as renderer from 'react-test-renderer';
import FooterItem from '../../footer-item';
const mockRequest = buildMockRequest();
const mockContext = {
    request: mockRequest
};
const mockHeading = {
    text: 'Mock heading'
};
const mockImage = {
    src: 'https://img-prod-cms-mr-microsoft-com.akamaized.net/cms/api/fabrikam/imageFileData/MA1QsS',
    altText: 'Mock Text'
};
const mockLink = {
    linkText: 'Mock action link 1 title',
    linkUrl: {
        destinationUrl: 'Mock action link 1 href',
    },
    ariaLabel: 'Mock action link 1 alt text'
};
const mockConfig = {
    heading: mockHeading,
    link: mockLink,
    image: mockImage,
    text: 'Mock text'
};
describe('FooterItem', () => {
    let moduleProps;
    beforeAll(() => {
        moduleProps = buildMockModuleProps({}, {}, mockConfig, mockContext);
    });
    it('renders correctly', () => {
        const component = renderer.create(React.createElement(FooterItem, Object.assign({}, moduleProps)));
        const tree = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
});
//# sourceMappingURL=footer-item.test.js.map