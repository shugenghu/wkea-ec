import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { FooterItemView } from '../../footer-item.view';
describe('footer item view tests', () => {
    it('renders correctly', () => {
        const moduleConfig = {
            className: 'class-mock'
        };
        const moduleProps = buildMockModuleProps({}, {}, moduleConfig);
        const viewprops = {
            ...moduleProps,
            heading: '{heading}',
            text: '{text}',
            image: '{image}',
            linkText: 'link text',
            FooterItem: { moduleProps, className: 'module-class-FooterItem' },
            Link: { className: 'mock-class-Link' },
            HeadingContainer: { className: 'mock-class-HeadingContainer' },
            ItemContainer: { className: 'mock-class-ItemContainer' },
            EditableLink: '{link}'
        };
        const view = render(React.createElement(FooterItemView, Object.assign({}, viewprops)));
        expect(view).toMatchSnapshot();
    });
    it('renders correctly without heading', () => {
        const moduleConfig = {
            className: 'class-mock'
        };
        const moduleProps = buildMockModuleProps({}, {}, moduleConfig);
        const viewprops = {
            ...moduleProps,
            heading: '',
            text: '{text}',
            image: '{image}',
            linkText: 'link text',
            FooterItem: { moduleProps, className: 'module-class-FooterItem' },
            Link: { className: 'mock-class-Link' },
            HeadingContainer: { className: 'mock-class-HeadingContainer' },
            ItemContainer: { className: 'mock-class-ItemContainer' },
            EditableLink: '{link}'
        };
        const view = render(React.createElement(FooterItemView, Object.assign({}, viewprops)));
        expect(view).toMatchSnapshot();
    });
    it('renders correctly without heading & image', () => {
        const moduleConfig = {
            className: 'class-mock'
        };
        const moduleProps = buildMockModuleProps({}, {}, moduleConfig);
        const viewprops = {
            ...moduleProps,
            heading: '',
            text: '{text}',
            image: '',
            linkText: 'link text',
            FooterItem: { moduleProps, className: 'module-class-FooterItem' },
            Link: { className: 'mock-class-Link' },
            HeadingContainer: { className: 'mock-class-HeadingContainer' },
            ItemContainer: { className: 'mock-class-ItemContainer' },
            EditableLink: '{link}'
        };
        const view = render(React.createElement(FooterItemView, Object.assign({}, viewprops)));
        expect(view).toMatchSnapshot();
    });
});
//# sourceMappingURL=footer-item.view.test.js.map