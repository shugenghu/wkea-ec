import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const PromoBannerView = props => {
    const { promoBannerProps, contentProps, closeButton, bannerMessages, PromoBannerTextProps, PromoBannerLinksProps, CarouselProps, handleLinks, handleText } = props;
    const slides = bannerMessages && bannerMessages.map((message, index) => {
        return (React.createElement(Node, Object.assign({}, contentProps, { key: index }),
            message.text && React.createElement(Node, Object.assign({}, PromoBannerTextProps, { text: message.text, onClick: handleText })),
            message.links && message.links.length > 0 && React.createElement(Node, Object.assign({}, PromoBannerLinksProps, { links: message.links, onClick: handleLinks }))));
    });
    return (React.createElement(Module, Object.assign({}, promoBannerProps),
        React.createElement(Node, Object.assign({}, CarouselProps, { items: slides })),
        closeButton));
};
export default PromoBannerView;
//# sourceMappingURL=promo-banner.view.js.map