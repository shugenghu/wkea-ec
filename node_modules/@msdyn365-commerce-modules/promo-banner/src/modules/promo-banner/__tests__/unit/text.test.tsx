/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
import { buildMockRequest } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { PromoBannerText } from '../../components';

describe('PromoBanner unit tests - Text', () => {
    it('renders correctly', () => {
        const mockProps = {
            text: 'Test string',
            requestContext: buildMockRequest(),
            onTextChange: jest.fn()
        };
        const component = render(<PromoBannerText {...mockProps} />);
        expect(component).toMatchSnapshot();
    });
});
