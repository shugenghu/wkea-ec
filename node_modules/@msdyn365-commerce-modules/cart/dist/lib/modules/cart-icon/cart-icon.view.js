import { Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const _renderCartlines = (cartLines, props) => {
    if (!cartLines) {
        props.context.telemetry.error('Cartlines content is empty, module wont render');
        return null;
    }
    return cartLines.map((cartLine, index) => {
        return (React.createElement(Node, Object.assign({}, props.miniCartItemWrapper, { key: index }),
            cartLine.cartline,
            cartLine.storeLocation,
            cartLine.remove));
    });
};
const CartIconView = (props) => (React.createElement(Node, Object.assign({}, props.miniCartWrapper, props.renderModuleAttributes(props)),
    props.cartIcon,
    props.FlyoutContainer ? (React.createElement(Node, Object.assign({}, props.FlyoutContainer),
        props.flyoutTitle,
        React.createElement(Node, Object.assign({}, props.CartlinesWrapper), _renderCartlines(props.cartlines, props)),
        props.totalPrice,
        props.checkoutAsSignInUserButton,
        props.goToCartButton)) : false));
export default CartIconView;
//# sourceMappingURL=cart-icon.view.js.map