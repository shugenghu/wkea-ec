import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { Heading } from '@msdyn365-commerce-modules/utilities';
import { render } from 'enzyme';
import * as React from 'react';
import IFrameView from '../../iframe.view';
describe('Iframe', () => {
    it('renders correctly with full screen', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const heading = {
            tag: "h2",
            text: 'mock heading'
        };
        const headingtag = (React.createElement(Heading, Object.assign({}, heading, { className: 'ms-iframe__heading__title' })));
        const iframe = (React.createElement("iframe", { className: 'mock ms-iframe-content', src: 'https://ppe-streaming-video-mr-microsoft-com.akamaized.net/5b4aaa0e-602a-4964-80d0-cdea46ec368f/1c372c3e-debb-42a3-a6db-58904cebdaad.mp4', height: '700px', "aria-label": 'iframe' }));
        const mockProps = {
            ...moduleProps,
            Heading: headingtag,
            IframeContainer: {
                moduleProps,
                className: 'mock ms-iframe',
            },
            IFrame: iframe,
        };
        const component = render(React.createElement(IFrameView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=iframe.view.test.js.map