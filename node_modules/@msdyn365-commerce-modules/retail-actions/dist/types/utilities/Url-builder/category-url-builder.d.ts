import { CategoryHierarchy } from '@msdyn365-commerce/commerce-entities';
import { IActionContext, IParameterizeFeatureFlags } from '@msdyn365-commerce/core';
interface ICategoryMap {
    [RecordId: number]: CategoryHierarchy;
}
/**
 * Builds slugs for category
 */
export declare function getCategorySlug(category: CategoryHierarchy, categoryMap: ICategoryMap, parameterizeFeatureFlags?: IParameterizeFeatureFlags): string;
/**
 * Gets Url for category hierarchy. Returns string and adds url to Url property of category hierarchy
 * @param category Category to generate URL for
 * @param categoryMap Dictionary of all categories
 */
export declare function getCategoryUrl(category: CategoryHierarchy, ctx?: IActionContext, categoryMap?: ICategoryMap): string | undefined;
export {};
