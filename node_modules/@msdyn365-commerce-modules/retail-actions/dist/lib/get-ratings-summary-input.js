/**
 * ReviewsGetRatingsSummary Input Action
 */
export class GetRatingsSummaryInput {
    constructor(productId, tenantId, serviceEndpoint) {
        // RnR does not use RS endpoint, so shouldn't use buildCacheKey
        this.getCacheKey = () => `${this.productId}-${this.tenantId}`;
        this.getCacheObjectType = () => 'RatingsSummary';
        this.dataCacheType = () => 'application';
        this.productId = productId;
        this.tenantId = tenantId;
        this.serviceEndpoint = serviceEndpoint;
    }
}
//# sourceMappingURL=get-ratings-summary-input.js.map