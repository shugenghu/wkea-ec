/**
 * Generic input type for use with generic data actions
 */
export class GenericInput {
    constructor(cacheKey, result, cacheObjectType) {
        this.getCacheKey = () => `Generic-${this.cacheKey}`;
        this.getCacheObjectType = () => `${this.cacheObjectType}`;
        this.dataCacheType = () => 'none';
        this.cacheKey = cacheKey;
        this.cacheObjectType = cacheObjectType;
        this.result = result;
    }
}
/**
 * Generic data action that is just used for passing data around the app
 *
 * Note that this doesn't actually expose a data action, its just a base action
 * that others can use to create a data action with their own input
 */
export async function getGenericAction(input, ctx) {
    return input.result;
}
//# sourceMappingURL=generic-data-action.js.map