import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { ProductCollectionView } from '../../..';
describe('Product Collection unit tests - View', () => {
    it('renders correctly', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const mockProps = {
            ...moduleProps,
            className: 'className',
            heading: React.createElement("div", null, "Trending now"),
            products: [],
            ProductCollectionContainer: { moduleProps, className: 'ms-product-collection' },
            SingleSlideCarouselComponentProps: { className: 'node-class-SingleSlideCarouselComponent' },
            GridComponentProps: { className: 'node-class-grid' },
            isCarousel: true
        };
        const component = render(React.createElement(ProductCollectionView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=product-collection.view.test.js.map