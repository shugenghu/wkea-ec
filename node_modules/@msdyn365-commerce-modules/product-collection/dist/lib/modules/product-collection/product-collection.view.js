import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
const ProductCollectionView = props => {
    const { heading, ProductCollectionContainer, products, SingleSlideCarouselComponentProps, GridComponentProps, isCarousel } = props;
    if (products) {
        return (React.createElement(Module, Object.assign({}, ProductCollectionContainer),
            heading,
            isCarousel ? _renderCarousel(SingleSlideCarouselComponentProps, products) : _renderGrid(GridComponentProps, products)));
    }
    props.context.telemetry.error('Product collection content is empty, module wont render.');
    return null;
};
const _renderCarousel = (carouselContainer, items) => {
    return (React.createElement(Node, Object.assign({}, carouselContainer), items && items.map(_renderProduct)));
};
const _renderGrid = (gridContainer, items) => {
    return (React.createElement(Node, Object.assign({}, gridContainer), items && items.map(_renderProduct)));
};
const _renderProduct = (product) => {
    const { ProductContainer, productComponent } = product;
    return (React.createElement(Node, Object.assign({}, ProductContainer), productComponent));
};
export default ProductCollectionView;
//# sourceMappingURL=product-collection.view.js.map