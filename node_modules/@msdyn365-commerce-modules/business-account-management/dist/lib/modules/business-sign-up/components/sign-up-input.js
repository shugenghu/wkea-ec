import * as React from 'react';
import { InputType } from '../business-sign-up';
const GetMaxLength = (maxLength) => {
    if (maxLength) {
        const parsedMaxLength = parseInt(maxLength, 10);
        if (!isNaN(parsedMaxLength)) {
            return parsedMaxLength;
        }
    }
    return undefined;
};
export const SignUpInput = ({ id, type, value, pattern, className, maxLength, isRequired, onChange }) => {
    if (type === InputType.TextArea) {
        return (React.createElement("textarea", { id: id, name: id, value: value, className: `${className}-input-area ${className}-${id}`, "aria-required": isRequired, required: isRequired, maxLength: GetMaxLength(maxLength), onChange: onChange }));
    }
    return (React.createElement("input", { id: id, type: type, value: value, className: `${className}-input ${className}-${id}`, pattern: pattern !== '' ? pattern : undefined, "aria-required": isRequired, required: isRequired, maxLength: GetMaxLength(maxLength), onChange: onChange }));
};
export default SignUpInput;
//# sourceMappingURL=sign-up-input.js.map