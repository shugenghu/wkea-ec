import { Button, getPayloadObject, getTelemetryAttributes } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const SignUpButton = ({ id, className, text, ariaLabel, telemetryContent, disabled, link, onClick }) => {
    const payLoad = getPayloadObject('click', telemetryContent, text);
    const attributes = getTelemetryAttributes(telemetryContent, payLoad);
    return (React.createElement(Button, Object.assign({ id: id, className: className, "aria-label": ariaLabel, disabled: disabled, onClick: onClick, href: link }, attributes), text));
};
export default SignUpButton;
//# sourceMappingURL=sign-up-button.js.map