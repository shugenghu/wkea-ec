import * as React from 'react';
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
const BusinessSignUpItem = ({ wrapper, label, errorMessage, input, isAddressInput }) => {
    return (React.createElement(Node, Object.assign({}, wrapper),
        !isAddressInput && label,
        !isAddressInput && errorMessage,
        input));
};
const BusinesSignUpView = props => {
    const { businessSignUp, ContainerWrapper, heading, businessSignUpForm } = props;
    const { FormWrapper, buttons, items, errorMessage, disclaimer } = businessSignUpForm;
    return (React.createElement(Module, Object.assign({}, businessSignUp),
        React.createElement(Node, Object.assign({}, ContainerWrapper),
            heading,
            React.createElement(Node, Object.assign({}, FormWrapper),
                items && items.map((item) => {
                    return (React.createElement(BusinessSignUpItem, Object.assign({ key: item.id }, item)));
                }),
                errorMessage && errorMessage.map((error, index) => {
                    return (React.createElement(React.Fragment, { key: index }, error));
                }),
                buttons && buttons.map((button, index) => {
                    return (React.createElement(React.Fragment, { key: index }, button));
                }),
                disclaimer))));
};
export default BusinesSignUpView;
//# sourceMappingURL=business-sign-up.view.js.map