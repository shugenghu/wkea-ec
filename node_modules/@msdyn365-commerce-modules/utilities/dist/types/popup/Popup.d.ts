import { IComponentProps, IGridSettings, IImageSettings } from '@msdyn365-commerce/core';
import { ProductPrice, SimpleProduct } from '@msdyn365-commerce/retail-proxy';
import * as React from 'react';
import { ITelemetryContent } from '..';
export interface IPopupProps extends IComponentProps<{
    product: SimpleProduct | undefined;
    price: ProductPrice | undefined;
}> {
    dialogStrings?: {
        goToCartText: string;
        continueShoppingText: string;
        headerItemOneText: string;
        headerItemFormatText: string;
        headerMessageText: string;
        freePriceText: string;
        originalPriceText: string;
        currentPriceText: string;
    };
    className: string;
    imageSettings?: IImageSettings;
    gridSettings: IGridSettings | undefined;
    productQuantity: number;
    priceComponent: React.ReactNode;
    navigationUrl?: string;
    modalOpen: boolean | undefined;
    telemetryContent?: ITelemetryContent;
    setModalOpen(newValue: boolean): void;
}
export declare const Popup: (popupProps: IPopupProps) => JSX.Element | null;
export default Popup;
