import { ITelemetryContent } from '../common/telemetryHelper';
export interface ISingleSlideCarouselProps {
    flipperNextLabel?: string;
    flipperPrevLabel?: string;
    vertical?: boolean;
    touchScrollThreshold?: number;
    parentId?: string;
    useTabList?: boolean;
    telemetryContent?: ITelemetryContent;
}
