import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { SearchResultContainerView } from '../../..';
describe('Search result container tests - View', () => {
    it('renders correctly', () => {
        const moduleProps = buildMockModuleProps({}, {});
        const mockTitle = {
            TitleContainer: { className: 'node-class-TitleContent' },
            title: {
                titlePrefix: React.createElement("span", null, " results for "),
                titleText: React.createElement("span", null, " Products "),
                titleCount: React.createElement("span", null, " 97 ")
            }
        };
        const mockProps = {
            ...moduleProps,
            className: 'className',
            products: React.createElement("div", null, "Products"),
            TitleViewProps: mockTitle,
            SearchResultContainer: { moduleProps, className: 'module-class' },
        };
        const component = render(React.createElement(SearchResultContainerView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=search-result-container.view.test.js.map