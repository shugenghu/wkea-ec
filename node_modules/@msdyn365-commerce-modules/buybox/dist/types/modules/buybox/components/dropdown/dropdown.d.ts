import * as React from 'react';
import { IDropdownProps } from './';
declare type IDropdownState = {
    selectedIndex: string;
};
export declare class Dropdown extends React.PureComponent<IDropdownProps, IDropdownState> {
    private selectMenu;
    private payLoad;
    constructor(props: IDropdownProps, state: IDropdownState);
    componentDidMount(): void;
    render(): JSX.Element;
    private _onChanged;
}
export {};
