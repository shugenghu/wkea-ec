import { render } from 'enzyme';
import * as React from 'react';
import { buildMockModuleProps } from '@msdyn365-commerce/core';
import HeaderModule from '../header';
import HeaderView from '../header.view';
import * as MockUtillities from '../utilities/mock-utilities';
const mockActions = {};
describe('Header integration tests', () => {
    it('renders correctly if user is sign-in', () => {
        const moduleProps = {
            ...buildMockModuleProps(MockUtillities.getMockData(0, undefined, 'firstName'), mockActions, MockUtillities.mockHeaderConfig, MockUtillities.mockAuthContext),
            resources: MockUtillities.mockResources,
            renderView: HeaderView,
        };
        const component = render(React.createElement(HeaderModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
    it('renders correctly if user is not sign-in', () => {
        const moduleProps = {
            ...buildMockModuleProps(MockUtillities.getMockErrorData(), mockActions, MockUtillities.mockHeaderConfig, MockUtillities.mockAuthContext),
            resources: MockUtillities.mockResources,
            renderView: HeaderView,
        };
        const component = render(React.createElement(HeaderModule, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=integration.test.js.map