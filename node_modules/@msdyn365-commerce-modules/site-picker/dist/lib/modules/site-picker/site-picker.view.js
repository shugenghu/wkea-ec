import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export default (props) => {
    const { SitePicker, SitePickerMenu, SitePickerMenuSpan } = props;
    return (React.createElement(Module, Object.assign({}, SitePicker),
        React.createElement(Node, Object.assign({}, SitePickerMenu, { onClick: props.renderSiteOptions }),
            props.config.heading?.text,
            React.createElement(Node, Object.assign({}, SitePickerMenuSpan))),
        props.siteOptionsState ? _renderSiteOptionsList(props) : null));
};
const _renderSiteOptionsList = (props) => {
    const { SitePickerMenuList, SitePickerMenuListItem, SitePickerMenuListItemArray, } = props;
    const options = SitePickerMenuListItemArray.map((item, index) => {
        return (React.createElement(Node, Object.assign({}, SitePickerMenuListItem, { key: index }), item));
    });
    return (React.createElement(Node, Object.assign({}, SitePickerMenuList, { onMouseLeave: props.resetSiteOptionsState }), options));
};
//# sourceMappingURL=site-picker.view.js.map