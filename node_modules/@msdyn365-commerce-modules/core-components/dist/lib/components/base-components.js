/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { Text } from '@msdyn365-commerce/core';
import * as React from 'react';
const getContainerType = (containerType) => {
    switch (containerType) {
        case 'container':
            return 'container';
        case 'fluid':
        default:
            return 'fluid';
    }
};
export const Container = (props) => {
    const { containerType, className, typeName, id, children } = props;
    const customClassName = className ? ` ${className}` : '';
    return containerType ? (React.createElement("div", { className: `${typeName} ${id} ${getContainerType(containerType)}${customClassName}` },
        React.createElement("div", { className: 'row' }, children))) : (React.createElement("div", { className: `${typeName} ${id}${customClassName}` }, children));
};
export const HeadingComponent = (props) => {
    const { tag, headingText, textSize, className } = props;
    const Tag = tag || 'h2';
    const classNameWithHeading = textSize ? `heading ${textSize} ${className}` : `heading ${className}`;
    return ((headingText && (React.createElement(Text, { className: classNameWithHeading, tag: Tag, text: headingText, editProps: { onEdit: props.handleTextChange, requestContext: props.requestContext } }))) ||
        null);
};
//# sourceMappingURL=base-components.js.map