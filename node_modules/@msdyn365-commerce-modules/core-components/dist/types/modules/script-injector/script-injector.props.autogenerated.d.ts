import * as Msdyn365 from '@msdyn365-commerce/core';
export declare const enum loadPoint {
    headStart = "headStart",
    headEnd = "headEnd",
    bodyStart = "bodyStart",
    bodyEnd = "bodyEnd"
}
export interface IScriptInjectorConfig {
    scriptSource?: string;
    async?: boolean;
    defer?: boolean;
    loadPoint?: loadPoint;
}
export interface IScriptInjectorProps<T> extends Msdyn365.IModule<T> {
    config: IScriptInjectorConfig;
}
