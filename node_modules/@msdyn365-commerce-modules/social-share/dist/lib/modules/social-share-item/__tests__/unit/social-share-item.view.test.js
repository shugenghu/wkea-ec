import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { SocialShareItemView } from '../../social-share-item.view';
const moduleProps = buildMockModuleProps({}, {});
const mockProps = {
    ...moduleProps,
    SocialShareItem: {
        moduleProps,
        className: 'ms-social-item-content'
    },
    SocialShareItemElement: React.createElement("div", null),
};
const mockProps1 = {
    ...moduleProps,
    SocialShareItem: {
        moduleProps,
        className: 'ms-social-item-content'
    },
    SocialShareItemElement: null,
};
describe('tab-item unit tests - View', () => {
    it('renders content', () => {
        const component = render(React.createElement(SocialShareItemView, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
    it('renders null', () => {
        const component = render(React.createElement(SocialShareItemView, Object.assign({}, mockProps1)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=social-share-item.view.test.js.map