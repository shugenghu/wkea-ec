import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import SocialShareItem from '../social-share-item';
import SocialShareItemView from '../social-share-item.view';
const mockConfig = {
    socialMedia: "facebook",
    className: '',
    icon: {},
};
const mockConfig1 = {
    socialMedia: "linkedin",
    icon: {},
    className: 'ms-accordion-item-content'
};
const myReactNodeChild1 = 'whatever child module 1';
const slots = {
    accordionItemContent: [myReactNodeChild1]
};
describe('SocialShareItem', () => {
    it('renders correctly', () => {
        let moduleProps;
        moduleProps = {
            slots: slots,
            ...buildMockModuleProps({}, mockConfig),
            renderView: SocialShareItemView
        };
        const component = render(React.createElement(SocialShareItem, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
describe('SocialShareItemWithCustomeURL', () => {
    it('renders correctly', () => {
        let moduleProps;
        moduleProps = {
            slots: slots,
            ...buildMockModuleProps({}, mockConfig1),
            renderView: SocialShareItemView
        };
        const component = render(React.createElement(SocialShareItem, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=integration.test.js.map