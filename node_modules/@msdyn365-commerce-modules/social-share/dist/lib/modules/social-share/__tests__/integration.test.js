import { buildMockModuleProps } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import SocialShare from '../social-share';
import SocialShareView from '../social-share.view';
const mockAccordionData = {};
const mockConfigWithFlipper = {
    className: 'className',
    orientation: "horizontal"
};
const mockConfig2 = {
    caption: 'Share:',
    className: 'className',
    orientation: "horizontal"
};
const myReactNodeChild1 = 'whatever child module 1';
const myReactNodeChild2 = 'whatever child module 2';
const myReactNodeChild3 = 'whatever child module 3';
const myReactNodeChild4 = 'whatever child module 4';
const slots = {
    socialShareItems: [myReactNodeChild1, myReactNodeChild2],
    socialShareCustomizationItems: [myReactNodeChild3, myReactNodeChild4]
};
describe('Social Share integration tests', () => {
    it('renders correctly', () => {
        const moduleProps = {
            ...buildMockModuleProps(mockAccordionData, mockConfigWithFlipper),
            slots: slots,
            renderView: SocialShareView
        };
        const component = render(React.createElement(SocialShare, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
    it('renders correctly', () => {
        const moduleProps = {
            ...buildMockModuleProps(mockAccordionData, mockConfig2),
            slots: slots,
            renderView: SocialShareView
        };
        const component = render(React.createElement(SocialShare, Object.assign({}, moduleProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=integration.test.js.map