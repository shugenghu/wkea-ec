import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import React from 'react';
export const SocialShareView = props => {
    const { SocialShareList, SocialShareItemElements, SocialShareItem, SocialShare } = props;
    if (SocialShareItemElements.length === 0) {
        props.context.telemetry.error('Social media list is empty, module wont render');
        return null;
    }
    return (React.createElement(Module, Object.assign({}, SocialShare),
        React.createElement(Node, Object.assign({}, SocialShareList), SocialShareItemElements.map((SocialShareElement, index) => {
            return (React.createElement(Node, Object.assign({}, SocialShareItem, { key: index }), SocialShareElement));
        }))));
};
export default SocialShareView;
//# sourceMappingURL=social-share.view.js.map