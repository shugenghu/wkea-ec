import { IActionContext } from '@msdyn365-commerce/core';
import { BaseStoreSelectorStateManager } from './base-store-selector-state-manager';
import { ILoadMapApiInput, IStoreSelectorLocation } from './i-store-selection-state-manager';
/**
 * Instance of IStoreSelectorStateManager manager that uses Map service
 * for geo location
 */
export declare class MapStoreSelectorStateManager extends BaseStoreSelectorStateManager {
    private readonly SERVICE_ENDPOINT;
    private readonly MAP_API_ENDPOINT;
    geoLocate(searchTerm: string, actionContext: IActionContext): Promise<IStoreSelectorLocation | undefined>;
    loadMapApi(input: ILoadMapApiInput): Promise<void>;
}
