import { IActionContext } from '@msdyn365-commerce/core';
import { IListMapViewState, ILoadMapApiInput, IStoreSelectionStateContext, IStoreSelectorLocation, IStoreSelectorStateManager } from './i-store-selection-state-manager';
/**
 * Abstract instance of IStoreSelectorStateManager with base implementation
 * of everything except the geoLocate function
 */
export declare abstract class BaseStoreSelectorStateManager implements IStoreSelectorStateManager {
    get isDialogOpen(): Readonly<boolean>;
    isMapApiLoaded?: boolean;
    selectedStoreLocationId?: string;
    preferredStoreLocationId?: string;
    listMapViewState: IListMapViewState;
    protected _context?: IStoreSelectionStateContext;
    get context(): Readonly<IStoreSelectionStateContext | undefined>;
    abstract geoLocate(searchTerm: string, actionContext: IActionContext): Promise<IStoreSelectorLocation | undefined>;
    abstract loadMapApi(input: ILoadMapApiInput): Promise<void>;
    toggleListMapViewState(): Promise<void>;
    setOrgUnitStoreInformation(input: IStoreSelectionStateContext): Promise<void>;
    setSelectedStoreLocationId(storeId: string | undefined): Promise<void>;
    setPreferredStoreLocationId(storeId: string | undefined): Promise<void>;
    openDialog(input: IStoreSelectionStateContext): Promise<void>;
    closeDialog(): Promise<void>;
}
