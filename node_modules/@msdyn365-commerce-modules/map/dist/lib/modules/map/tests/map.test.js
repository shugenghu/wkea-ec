import { buildMockModuleProps } from '@msdyn365-commerce/core-internal';
import * as React from 'react';
import * as renderer from 'react-test-renderer';
import Map from '../map';
const mockData = {
    actionResponse: {
        text: 'Sample Response Data'
    },
    storeSelectorStateManager: {
        status: 'SUCCESS',
        result: {}
    },
};
const mockConfig = {
    heading: {
        text: 'Map'
    },
    className: 'testClassName'
};
const mockActions = {};
describe('StoreLocator', () => {
    let moduleProps;
    beforeAll(() => {
        moduleProps = buildMockModuleProps(mockData, mockActions, mockConfig);
    });
    it('renders correctly', () => {
        const component = renderer.create(React.createElement(Map, Object.assign({}, moduleProps)));
        const tree = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
});
//# sourceMappingURL=map.test.js.map