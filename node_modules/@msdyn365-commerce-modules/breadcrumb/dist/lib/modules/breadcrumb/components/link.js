import { getPayloadObject, getTelemetryAttributes, onTelemetryClick } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
export const Link = ({ text, className, href, ariaLabel, index, noclick, telemetryContent }) => {
    const payLoad = getPayloadObject('click', telemetryContent, '', '');
    const linkText = text !== undefined ? text : '';
    payLoad.contentAction.etext = linkText;
    const attribute = getTelemetryAttributes(telemetryContent, payLoad);
    return (React.createElement("a", Object.assign({}, attribute, { className: className, href: href, "aria-label": ariaLabel, onClick: onTelemetryClick(telemetryContent, payLoad, linkText) }, (noclick && { 'data-no-click': true })),
        React.createElement("span", { itemProp: 'name' }, linkText),
        React.createElement("meta", { itemProp: 'position', content: index })));
};
//# sourceMappingURL=link.js.map