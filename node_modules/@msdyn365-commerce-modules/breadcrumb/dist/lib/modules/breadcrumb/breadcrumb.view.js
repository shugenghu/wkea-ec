/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { Module, Node } from '@msdyn365-commerce-modules/utilities';
import classnames from 'classnames';
import * as React from 'react';
const BreadCrumbView = props => {
    const { root, links, backButton, BreadcrumbContainer, BreadcrumbList, BreadcrumbListItem, separator } = props;
    if (!root && !links && !backButton) {
        props.context.telemetry.error('BreadCrumb content is empty, module wont render');
        return null;
    }
    return (React.createElement(Module, Object.assign({}, BreadcrumbContainer),
        React.createElement(Node, Object.assign({}, BreadcrumbList),
            root &&
                React.createElement(Node, Object.assign({}, BreadcrumbListItem),
                    root,
                    links && separator),
            backButton &&
                React.createElement(Node, Object.assign({}, BreadcrumbListItem), backButton),
            links && links.map((link, index) => (React.createElement(React.Fragment, { key: index },
                React.createElement(Node, Object.assign({}, BreadcrumbListItem, { className: classnames(BreadcrumbListItem.className, index === links.length - 1 ? 'breadcrumb_item-current' : '') }),
                    link,
                    links[index + 1] && separator)))))));
};
export default BreadCrumbView;
//# sourceMappingURL=breadcrumb.view.js.map