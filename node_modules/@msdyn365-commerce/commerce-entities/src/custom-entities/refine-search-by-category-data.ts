import { ProductRefinerValue, ProductSearchResult } from '@msdyn365-commerce/retail-proxy';

/**
 * The output of the refiners-search-by-category action includes both the set of refined products
 * as well as the input refinement criteria used to refine those products.
 */
export interface IRefineSearchByCategoryData {
    refinementCriteria: ProductRefinerValue[];
    productSearchResult: ProductSearchResult[];
}