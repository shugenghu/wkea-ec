import { SimpleProduct } from '@msdyn365-commerce/retail-proxy';

/**
 * A Hydrated product that contains extra rendering friendly fields
 */
export interface IHydratedProduct extends SimpleProduct {
    ImageUrl?: string;
}