import { ProductRefinerValue } from '@msdyn365-commerce/retail-proxy';
import { FullProduct } from './full-product';

/**
 * The output of the refiners-search actions includes both the set of refined products
 * as well as the input refinement criteria used to refine those products.
 */
export interface IRefineFullProductSearchOutput {
  productSearchResult: FullProduct[];
  refinementCriteria: ProductRefinerValue[];
}
