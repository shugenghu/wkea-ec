import { Cart, CartLine, SimpleProduct } from '@msdyn365-commerce/retail-proxy';
export interface HydratedCart extends Cart {
    CartLines: HydratedCartLine[];
}
export interface HydratedCartLine extends CartLine {
    FullProduct: SimpleProduct;
}
