/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { createObservableDataAction } from '../../create-data-action.vnext';
/**
 * test class
 */
/*@internal*/
export class GetActionTimeoutInput {
    constructor(timeout) {
        this.dataCacheType = () => 'application';
        this.getCacheObjectType = () => 'Timeout';
        this.getCacheKey = () => `${this.timeout}`;
        this.timeout = timeout;
    }
}
async function GetActionTimeout(input, ctx) {
    ctx.trace('Starting action timeout with delay of ' + input.timeout);
    return new Promise(resolve => {
        setTimeout(resolve.bind(resolve, input.timeout), input.timeout);
    });
}
const createTestInput = (arg) => {
    return new GetActionTimeoutInput((arg.config && arg.config.timeout) || 0);
};
/*@internal*/
export default createObservableDataAction({
    action: GetActionTimeout,
    input: createTestInput
});
//# sourceMappingURL=get-action-timeout.js.map