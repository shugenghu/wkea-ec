/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { createDataAction } from '../../create-data-action';
/*@internal*/
export class CartInput {
    constructor() {
        this.getCacheObjectType = () => 'CART';
        this.getCacheKey = () => 'USER_CART';
        this.shouldCacheOutput = () => true;
    }
    CartInput() {
        // ideally this class gets the context and reads the user id from there
    }
}
export const mockGetCart = jest.fn(getCart);
// todo: this could be from zone.current
const cartInfo = { lines: [] };
async function getCart(input, ctx) {
    return cartInfo;
}
/*@internal*/
//@ts-ignore
export default createDataAction({ action: mockGetCart, id: 'TestHook', input: () => new CartInput() });
//# sourceMappingURL=get-cart.js.map