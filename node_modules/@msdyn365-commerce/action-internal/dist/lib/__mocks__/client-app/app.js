/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { RequestCache } from '@msdyn365-commerce/cache-internal';
import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { ActionContext } from '../../action-context';
import ProductList from './product-list';
import ProductSearchList from './product-search';
const defaultActionContext = new ActionContext({
    id: 'client',
    requestContext: {},
    requestCache: new RequestCache(),
    appCache: null
});
// window['cache'] = cache;
class App extends React.Component {
    render() {
        return (React.createElement(React.Fragment, null,
            React.createElement("h2", null, "Action Layer Demo"),
            React.createElement(ProductList, { channel: 'top-free', actionContext: defaultActionContext }),
            React.createElement(ProductList, { channel: 'trending', actionContext: defaultActionContext }),
            React.createElement(ProductList, { channel: 'top-paid', actionContext: defaultActionContext }),
            React.createElement(ProductSearchList, { actionContext: defaultActionContext })));
    }
}
ReactDOM.render(React.createElement(App, null), document.getElementById('root'));
//# sourceMappingURL=app.js.map