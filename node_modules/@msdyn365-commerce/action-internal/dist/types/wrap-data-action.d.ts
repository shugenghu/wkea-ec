/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { IAction, IActionInput, IObservableAction } from '@msdyn365-commerce/core-internal';
export declare type dataActionOptions<TOut> = {
    action: IAction<TOut>;
    isBatched?: boolean;
    id?: string;
    input?: (...args: any[]) => IActionInput | IActionInput[];
};
declare type IActionWrap<TOut> = IAction<TOut> | IObservableAction<TOut>;
/**
 * Wraps a data action with the associated action wrap.
 * @param options
 * @param actionWrap
 */
export declare const wrapDataAction: <TOut>(options: dataActionOptions<TOut>, actionWrap: IActionWrap<TOut>) => IActionWrap<TOut>;
export {};
