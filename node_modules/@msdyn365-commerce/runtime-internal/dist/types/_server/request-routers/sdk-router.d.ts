export interface IFeatureFlags {
    features: {
        name: string;
        dependencies?: {
            name: string;
            minVersion?: string;
            maxVersion?: string;
        }[];
    }[];
    retailServerVersion?: string;
    SDKVersion?: string;
    SSKVersion?: string;
}
export default function (server: any): import("express-serve-static-core").Router;
