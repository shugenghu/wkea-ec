/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { IRefreshConfigs } from '@msdyn365-commerce/action-internal';
import { ICache, RequestCache } from '@msdyn365-commerce/cache-internal';
import { IPageConfig, IPageResponse, IRequestContext } from '@msdyn365-commerce/core-internal';
import { InternalTelemetry, ITelemetry } from '@msdyn365-commerce/telemetry-internal';
export interface IRenderDefaultRouterOptions {
    content: IPageResponse;
    options: {
        clientStats: {};
    };
    context: IRequestContext;
    telemetry: InternalTelemetry;
    moduleTelemetry: ITelemetry;
    pageConfig: IPageConfig;
    requestCache: RequestCache;
    routeRefreshActions: IRefreshConfigs[];
}
export interface IMakeLoadDataCallsOptions {
    content: IPageResponse;
    context: IRequestContext;
    internalTelemetry: InternalTelemetry;
    moduleTelemetry: ITelemetry;
    requestCache: ICache;
    appCache: ICache;
    allRefreshActions: IRefreshConfigs[];
}
declare const serverRenderVnext: ({ content, context, options: { clientStats }, pageConfig, telemetry, moduleTelemetry, requestCache, routeRefreshActions }: IRenderDefaultRouterOptions) => Promise<string>;
export default serverRenderVnext;
