/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { IAuthoringResourceDefinition, IDictionary, IPlatformSettings, IResourceManager } from '@msdyn365-commerce/core-internal';
/**
 * Resource manager implementation
 */
export declare class ResourceManager implements IResourceManager {
    private authoringResourcesMap;
    private moduleResourcesMap;
    private moduleResourceKeys;
    private moduleNamespaceMap;
    constructor();
    /**
     * @function        {init}          - Method to initialize and load all the resource strings
     */
    init(platformSettings: IPlatformSettings): Promise<void>;
    /**
     * @function    {getModuleResources}    - Method to fetch resource strings of a module for given locale
     * @param       {moduleName}            - name of the module
     * @param       {locale}                - locale for which resource strings need to be fetched
     */
    getModuleResources(moduleName: string, locale: string): IDictionary<string>;
    getAuthoringResources(): IDictionary<IAuthoringResourceDefinition>;
    private _readAuthoringResourceFiles;
    private _readModuleResourceFiles;
    private _getAllModuleDefinitions;
    private _getMergedAuthoringResources;
}
