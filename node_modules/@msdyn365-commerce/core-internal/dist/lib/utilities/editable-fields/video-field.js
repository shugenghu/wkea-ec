/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import React from 'react';
import { getEditableFieldClassName } from './utils';
export const VideoField = (props) => {
    const onKeyPress = (event) => {
        // 'enter' key
        if (event.charCode === 13 && props.onChange) {
            event.preventDefault();
            props.onChange(event);
        }
    };
    const editableProps = props.disabled
        ? {}
        : {
            onKeyPress: onKeyPress,
            onDoubleClick: props.onChange,
            onMouseOver: props.onMouseOver,
            onFocus: props.onFocus,
            onBlur: props.onBlur,
            onMouseOut: props.onMouseOut,
            tabIndex: 0
        };
    return (React.createElement("div", Object.assign({}, editableProps, { role: props.fieldProps.role || 'presentation', className: getEditableFieldClassName(props.disabled, props.fieldProps.className, props.requestContext) }), props.fieldProps.children));
};
//# sourceMappingURL=video-field.js.map