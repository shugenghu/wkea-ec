/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import * as React from 'react';
import { EditableField, FieldType } from './editable-fields/editable-field';
export function getRichTextHtml(text) {
    return text ? { __html: text } : undefined;
}
/**
 * Rich text interactive component
 */
export class RichTextComponent extends React.Component {
    constructor(props) {
        super(props);
        this.render = () => {
            return (React.createElement(EditableField, { onChange: this.props.editProps ? this.props.editProps.onEdit : this.onEdit, requestContext: this.props.editProps && this.props.editProps.requestContext, fieldProps: {
                    text: this.props.text,
                    className: this.props.className
                }, type: FieldType.RichText }));
        };
        this.onEdit = (event) => null;
        this.state = { html: props.text };
    }
}
//# sourceMappingURL=get-richtext-html.js.map