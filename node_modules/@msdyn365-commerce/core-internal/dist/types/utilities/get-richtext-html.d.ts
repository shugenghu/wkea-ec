/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import * as React from 'react';
import { RichText } from '../interfaces/data-types';
import { ITextEditProps } from './editable-fields/editable-field';
export interface IRichText {
    text?: RichText;
    className?: string;
    editProps?: ITextEditProps;
}
export declare function getRichTextHtml(text?: RichText): {
    __html: string;
} | undefined;
/**
 * Rich text interactive component
 */
export declare class RichTextComponent extends React.Component<IRichText> {
    constructor(props: IRichText);
    render: () => JSX.Element;
    private onEdit;
}
