/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { IGridSettings } from '../../contract/IGridSettings';
import { IImageData } from '../../interfaces/data-types/IImageData';
import { IImageDimension, IImageSettings } from '../../interfaces/data-types/IImageSettings';
import { Viewport } from './IImage';
export declare const getLargestGridSettingViewportName: (gridSettings: IGridSettings) => Viewport;
export declare const getImageResizeUrl: (imageSrc: string, vpSetting: IImageDimension | undefined, inputImageSettings?: IImageSettings | undefined) => string;
export declare const getSourceData: (src: string, gridSetting: IGridSettings, image: IImageData, imageSettings: IImageSettings, viewport: Viewport, largestGridSettingViewportName: Viewport) => {
    media: string;
    srcSet: string;
} | undefined;
