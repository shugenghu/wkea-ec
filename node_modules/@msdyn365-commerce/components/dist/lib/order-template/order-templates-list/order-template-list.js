import { Button, format } from '@msdyn365-commerce-modules/utilities';
import * as React from 'react';
import { OrderTemplateProductsPreviewComponent } from './order-template-products-preview';
export const OrderTemplateComponent = (props) => {
    const { orderTemplate, className, imageSettings } = props;
    const onActionButtonClick = () => {
        return props.orderTemplateAction(props.orderTemplate.productList);
    };
    const products = orderTemplate.orderTemplateLines.map(line => line.fullProduct).filter(fullProduct => fullProduct);
    return (React.createElement("div", { id: `${className}__id_${orderTemplate.productList.Id}`, className: `${className}__container` },
        React.createElement("div", { className: `${className}__item_header` },
            React.createElement("div", { className: `${className}__title` }, orderTemplate.productList.Name),
            React.createElement("div", { className: `${className}__count` }, format(props.lineItemsText, orderTemplate.totalLines))),
        React.createElement(OrderTemplateProductsPreviewComponent, { context: props.context, className: className, products: products, imageSettings: imageSettings }),
        React.createElement("div", { className: `${className}__footer` },
            React.createElement(Button, { className: `${className}__action-button`, title: props.actionButtonText, onClick: onActionButtonClick }, props.actionButtonText))));
};
//# sourceMappingURL=order-template-list.js.map