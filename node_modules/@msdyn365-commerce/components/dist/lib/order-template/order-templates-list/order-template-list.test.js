import { render } from 'enzyme';
import * as React from 'react';
import { buildMockCoreContext } from '@msdyn365-commerce/core';
import { OrderTemplateComponent } from './order-template-list';
describe('OrderTemplateComponent', () => {
    let mockCoreContext;
    const productListLine = {
        ProductListId: 'product-list-id',
        LineId: 'line-id',
        ProductId: 42,
        Quantity: 12,
        UnitOfMeasure: 'ea'
    };
    const orderTemplate = {
        productList: {
            Id: 'id',
            ProductListLines: [productListLine]
        },
        orderTemplateLines: [{ productListLine: productListLine }],
        totalLines: 0
    };
    const onOrderTemplateSelected = async () => {
        return;
    };
    beforeEach(() => {
        mockCoreContext = buildMockCoreContext({ app: { config: { enableStockCheck: false, outOfStockThreshold: 5 } } });
    });
    it('renders correctly with all params', () => {
        const orderTemplateComponent = render((React.createElement(OrderTemplateComponent, { context: mockCoreContext, className: 'class-name', orderTemplate: orderTemplate, actionButtonText: 'actionButtonText', lineItemsText: 'lineItemsText', orderTemplateAction: onOrderTemplateSelected })));
        expect(orderTemplateComponent).toMatchSnapshot();
    });
});
//# sourceMappingURL=order-template-list.test.js.map