import { render } from 'enzyme';
import * as React from 'react';
import { buildMockCoreContext } from '@msdyn365-commerce/core';
import { OrderTemplateNameDialog } from './order-template-name-dialog.component';
describe('OrderTemplateNameDialog', () => {
    let mockCoreContext;
    const createOrderTemplateDialogResources = {
        orderTemplateHeaderLabel: 'createOrderTemplateHeader',
        orderTemplateTitleLabel: 'orderTemplateTitle',
        orderTemplateNameAriaLabel: 'orderTemplateNameAriaLabel',
        orderTemplateDescription: 'createOrderTemplateDescription',
        defaultOrderTemplateName: 'defaultOrderTemplateName',
        orderTemplateButtonLabel: 'createOrderTemplateButtonText',
        orderTemplateCancelButtonLabel: 'cancelNewOrderTemplateCreationButtonText'
    };
    const setModalStatus = (newValue) => {
        return;
    };
    const onCancel = () => {
        return;
    };
    const onSuccess = async () => {
        return;
    };
    beforeEach(() => {
        mockCoreContext = buildMockCoreContext({ app: { config: { enableStockCheck: false, outOfStockThreshold: 5 } } });
    });
    it('renders correctly with all params', () => {
        const addToOrderTemplateDialog = render((React.createElement(OrderTemplateNameDialog, { id: 'id', typeName: 'type-name', context: mockCoreContext, data: {}, modalOpen: false, isUpdateDialog: false, setModalOpen: setModalStatus, className: 'class-name', dialogStrings: createOrderTemplateDialogResources, onCancel: onCancel, onSuccess: onSuccess })));
        expect(addToOrderTemplateDialog).toMatchSnapshot();
    });
    it('renders correctly with all params and modal open', () => {
        const addToOrderTemplateDialog = render((React.createElement(OrderTemplateNameDialog, { id: 'id', typeName: 'type-name', context: mockCoreContext, data: {}, modalOpen: true, isUpdateDialog: false, setModalOpen: setModalStatus, className: 'class-name', dialogStrings: createOrderTemplateDialogResources, onCancel: onCancel, onSuccess: onSuccess })));
        expect(addToOrderTemplateDialog).toMatchSnapshot();
    });
});
//# sourceMappingURL=order-template-name-dialog.test.js.map