import { render } from 'enzyme';
import * as React from 'react';
import Fade from './Fade';
describe('Fade', () => {
    it('renders basic Primary Fade correctly', () => {
        const fade = render(React.createElement("div", null,
            React.createElement("button", { color: 'primary' }, "Toggle Fade"),
            React.createElement(Fade, { tag: 'h5', className: 'mt-3' }, "This content will fade in and out as the button is pressed")));
        expect(fade).toMatchSnapshot();
    });
    it('renders basic Primary Fade correctly with added timeout prop', () => {
        const fade = render(React.createElement("div", null,
            React.createElement("button", { color: 'primary' }, "Toggle Fade"),
            React.createElement(Fade, { tag: 'h5', timeout: 50, className: 'mt-3' }, "This content will fade in and out as the button is pressed")));
        expect(fade).toMatchSnapshot();
    });
});
//# sourceMappingURL=Fade.test-ssr.js.map