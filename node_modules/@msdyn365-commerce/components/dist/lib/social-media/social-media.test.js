import { buildMockCoreContext } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { SocialMedia, SocialMediaComponent } from './social-media.components';
describe('Cart icon tests', () => {
    it('renders correctly', () => {
        const imageData = {
            src: 'https://cms-ppe-imageresizer-mr.trafficmanager.net/cms/api/gfhwnkhdlh/imageFileData/MC3EAQ'
        };
        const mockProps = {
            context: buildMockCoreContext({}),
            data: {},
            id: 'id',
            typeName: 'typeName',
            postUrl: 'https://www.microsoft.com/en-us/',
            socialMedia: SocialMedia.Facebook,
            src: imageData
        };
        const component = render(React.createElement(SocialMediaComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=social-media.test.js.map