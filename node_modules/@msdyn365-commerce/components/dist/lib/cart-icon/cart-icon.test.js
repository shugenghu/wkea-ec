import { buildMockCoreContext } from '@msdyn365-commerce/core';
import { render } from 'enzyme';
import * as React from 'react';
import { CartIconComponent } from './cart-icon.component';
describe('Cart icon tests', () => {
    it('renders correctly', () => {
        const mockProps = {
            context: buildMockCoreContext({}),
            data: { cart: { totalItemsInCart: 3 } },
            id: 'id',
            typeName: 'typeName',
            cartLabel: 'Shopping bag, {0} items',
            telemetryContent: {},
        };
        const component = render(React.createElement(CartIconComponent, Object.assign({}, mockProps)));
        expect(component).toMatchSnapshot();
    });
});
//# sourceMappingURL=cart-icon.test.js.map