import { render } from 'enzyme';
import * as React from 'react';
import { UncontrolledActionToggle } from './UncontrolledActionToggle';
describe('Example: UncontrolledActionToggle Play / Pause', () => {
    it('renders', () => {
        const actionToggle = render(React.createElement(UncontrolledActionToggle, { id: 'my-uncontrolled-action-toggle-glyphOnly' },
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-play' })),
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-pause' }))));
        expect(actionToggle).toMatchSnapshot();
    });
});
describe('Example: Expand/collapse text', () => {
    it('renders', () => {
        const actionToggle = render(React.createElement(UncontrolledActionToggle, { id: 'my-uncontrolled-action-toggle-button' },
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-play' })),
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-pause' }))));
        expect(actionToggle).toMatchSnapshot();
    });
});
//# sourceMappingURL=UncontrolledActionToggle.test-ssr.js.map