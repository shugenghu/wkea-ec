import { render } from 'enzyme';
import * as React from 'react';
import ActionToggle from './ActionToggle';
describe('ActionToggle', () => {
    it('does not render with less then two children', () => {
        const actionToggle = render(React.createElement(ActionToggle, { id: 'one-child' },
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-play' }))));
        expect(actionToggle).toMatchSnapshot();
    });
});
describe('ActionToggle', () => {
    it('renders untoggled correctly', () => {
        const actionToggle = render(React.createElement(ActionToggle, { id: 'untoggled' },
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-play' })),
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-pause' }))));
        expect(actionToggle).toMatchSnapshot();
    });
});
describe('ActionToggle', () => {
    it('renders toggled correctly', () => {
        const actionToggle = render(React.createElement(ActionToggle, { id: 'toggled', toggled: true },
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-play' })),
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-pause' }))));
        expect(actionToggle).toMatchSnapshot();
    });
});
describe('ActionToggle', () => {
    it('renders aria-hidden correctly', () => {
        const actionToggle = render(React.createElement(ActionToggle, { id: 'hidden', "aria-hidden": true },
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-play' })),
            React.createElement("button", { type: 'button', className: 'btn btn-secondary btn-lg rounded-circle border-light' },
                React.createElement("i", { className: 'fa fa-pause' }))));
        expect(actionToggle).toMatchSnapshot();
    });
});
//# sourceMappingURL=ActionToggle.test-ssr.js.map