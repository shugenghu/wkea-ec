import { KeycodesClass } from './KeyCodes';
describe('keycodes', () => {
    it('temp keycodes test for coverage', () => {
        const keyCode = new KeycodesClass();
        expect(keyCode.testCoverage()).toBe(':(');
    });
});
//# sourceMappingURL=KeyCodes.test.js.map