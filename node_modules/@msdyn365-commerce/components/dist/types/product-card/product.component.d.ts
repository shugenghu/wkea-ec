import { ITelemetryContent } from '@msdyn365-commerce-modules/utilities';
import { IComponent, IComponentProps, IImageSettings } from '@msdyn365-commerce/core';
import { ProductSearchResult } from '@msdyn365-commerce/retail-proxy';
import React from 'react';
export interface IProductComponentProps extends IComponentProps<{
    product?: ProductSearchResult;
}> {
    className?: string;
    imageSettings?: IImageSettings;
    savingsText?: string;
    freePriceText?: string;
    originalPriceText?: string;
    currentPriceText?: string;
    ratingAriaLabel?: string;
    allowBack?: boolean;
    telemetryContent?: ITelemetryContent;
}
export interface IProductComponent extends IComponent<IProductComponentProps> {
}
export declare const ProductComponent: React.FunctionComponent<IProductComponentProps>;
