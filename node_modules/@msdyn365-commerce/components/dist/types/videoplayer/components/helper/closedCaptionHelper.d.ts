import { IVideoMetadata } from '../../components/data/player-metadata';
import { ICurrentClosedCaptionsSettings, IPlayerMenu } from '../data/player-constants';
import LocalizationHelper from './localization-helper';
import PlayerHelper from './playerHelper';
export default class ClosedCaptionHelper {
    private playerMetaData;
    private playerHelper;
    private localizationHelper;
    constructor(props: IVideoMetadata, playerHelper: PlayerHelper, localizationHelper: LocalizationHelper);
    checkClosedCaptionsFiles(): boolean;
    getClosedCaptionMenu(): IPlayerMenu | null;
    getUpdatedClosedCaptionSetting(currentClosedCaptionsSetting: ICurrentClosedCaptionsSettings, settingKey: string, optionKey: string): ICurrentClosedCaptionsSettings;
    getClosedCaptionURLFromLocale(locale: string): string;
    private _getOptionValue;
    private _getClosedCaptionsSettingsMenu;
    private _getCCMenuItemId;
    private _getCCSettingsMenuId;
}
