import { IPlayerProps } from '../../players/player/Player.props';
import { IPlayerMenu } from '../data/player-constants';
import { IVideoFile, IVideoMetadata } from '../data/player-metadata';
import LocalizationHelper from './localization-helper';
export default class PlayerHelper {
    private playerProps;
    private localizationHelper;
    constructor(props: IPlayerProps, localizationHelper: LocalizationHelper);
    getMetadata(): IVideoMetadata;
    updatePlayerProps(props: IPlayerProps): void;
    getOptionMenu(): IPlayerMenu | null;
    getPosterFrameUrl(): string;
    getVideoFileToPlay(): IVideoFile;
    onErrorCallback: (errorType: string, message: string) => void;
    isEmptyOrUndefined(value?: string): boolean;
    addIdPrefix(childId: string): string;
    removeIdPrefix(childId: string): string;
    private _getAudioTracksMenu;
    private _clearVideoPlayerMetaData;
    private _setCCFiles;
    private _setAudioTracks;
    private _mapVideoFiles;
    private _getMediaTypeAndQuality;
    private _removeProtocolFromUrl;
    private _getVideoFileByQuality;
    private _getVideoFileByType;
}
