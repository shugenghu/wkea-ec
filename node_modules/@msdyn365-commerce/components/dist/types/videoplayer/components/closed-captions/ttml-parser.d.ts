import { IDictionaryStringString } from '../utilities/utilities';
import { ITtmlContext } from './ttml-context';
import { ITtmlSettings, TtmlSettings } from './ttml-settings';
export interface ITtmlEvent {
    time: number;
    element?: Element;
}
export declare class TtmlParser {
    private static mediaStart;
    private static mediaEnd;
    static getLocalTagName(node: Node): string;
    static getAttributeNS(element: Element, name: string, namespace: string): string;
    static parse(ttmlDocument: XMLDocument, settingsOverrides: ITtmlSettings): ITtmlContext;
    static applyInlineStyles(settings: TtmlSettings, styleSet: IDictionaryStringString, element: Element): void;
    private static _parseXml;
    private static _verifyRoot;
    private static _parseTtAttrs;
    private static _ensureRegions;
    private static _processAnonymousSpans;
    private static _applyTiming;
    private static _applyStyling;
    private static _applyStyle;
    private static _applyStylesheet;
    private static _isTagNS;
    private static _getFirstElementByTagNameNS;
}
