import * as React from 'react';
import { IComponent, IComponentProps, IImageData } from '@msdyn365-commerce/core';
export declare enum SocialMedia {
    Facebook = "facebook",
    Twitter = "twitter",
    Pinterest = "pinterest",
    Mail = "mail",
    Linkedin = "linkedin",
    Customization = "customization"
}
export interface ISocialMediaProps extends IComponentProps<{}> {
    src?: IImageData;
    postUrl: string;
    postTitle?: string;
    socialMedia: SocialMedia;
    socialShareURL?: string;
    socialMediaName?: string;
}
export interface ISocialMediaComponent extends IComponent<ISocialMediaProps> {
}
export declare const SocialMediaComponent: React.FC<ISocialMediaProps>;
