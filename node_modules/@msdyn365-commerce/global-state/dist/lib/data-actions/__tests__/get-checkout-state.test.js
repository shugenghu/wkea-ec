import 'jest';
import * as core from '@msdyn365-commerce/core';
import mockRequestContext from '../../__mocks__/request-context.json';
import { getCheckoutState } from '../get-checkout-state';
describe('getCheckoutState', () => {
    // Setup mocks
    beforeAll(() => {
        core.initializeMockApp();
    });
    beforeEach(() => {
        jest.resetAllMocks();
    });
    it('calls createCheckoutStateDataAction', async () => {
        const actionContext = core.buildMockActionContext({ requestContext: mockRequestContext });
        const result = await getCheckoutState(actionContext);
        expect(result).toBeDefined();
    });
});
//# sourceMappingURL=get-checkout-state.test.js.map