import 'jest';
import * as core from '@msdyn365-commerce/core';
import mockRequestContext from '../../__mocks__/request-context.json';
import createCheckoutStateDataAction from '../checkout-state-data-action';
describe('checkoutStateDataAction', () => {
    // Setup mocks
    beforeAll(() => {
        core.initializeMockApp();
    });
    beforeEach(() => {
        jest.resetAllMocks();
    });
    it('creates createCheckoutStateDataAction', async () => {
        const dataAction = createCheckoutStateDataAction;
        const actionContext = core.buildMockActionContext({ requestContext: mockRequestContext });
        const input = dataAction.prototype.inputFunc({ requestContext: actionContext.requestContext });
        expect(input).toBeDefined();
        expect(input.key).toBe('CHECKOUTSTATE');
    });
});
//# sourceMappingURL=checkout-state-data-action.test.js.map