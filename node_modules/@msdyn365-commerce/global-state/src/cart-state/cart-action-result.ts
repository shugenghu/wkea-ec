import { Cart } from '@msdyn365-commerce/retail-proxy/dist/Entities/CommerceTypes.g';
import { ICartActionResult } from '../state-interfaces/i-cart-state';

/**
 * Add Products To Cart Output which contains additional internal information
 * for usage by cart state but not intended to be exposed to modules
 */
export interface ICartActionResultWithCart extends ICartActionResult {
    readonly cart: Cart | undefined;
}