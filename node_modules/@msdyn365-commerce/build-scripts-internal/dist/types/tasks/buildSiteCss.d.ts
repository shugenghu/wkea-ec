/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */
import { Result } from 'node-sass';
import { IResult } from '../models/IResult';
/**
 * Current directory
 * @internal
 */
export declare const currentDir: string;
export declare const siteStyleEntryFolder: string;
export declare const pageStyleEntries: string;
export declare const outFolder: string;
export declare const createSassResult: (pathToEntryPoint: string, isDebug: boolean) => Promise<{
    outFile: string;
    result: Result;
}>;
/**
 * Creates two build targets - one for .min.css (dev) and one for .css (debug)
 * @param pathToEntryPoint entry point for a file
 */
export declare const buildSassResults: (pathToEntryPoint: string) => Promise<{
    outFile: string;
    result: Result;
}[]>;
/**
 * Writes the result to a file and sourcemap if it exists
 * @param sassResult the output form node-sas
 */
export declare const writeSassResultToFile: (sassResult: {
    outFile: string;
    result: Result;
}) => Promise<IResult<undefined>>;
declare const _default: () => Promise<IResult<undefined>>;
/**
 * This task build all site level css
 * /styles
 *      /{name1}.style.scss     <------------ {name1} is an entry point for site with name {name1}
 *      /{name2}.style.scss     <------------ {name2} is an entry point for site with name {name2}
 */
export default _default;
